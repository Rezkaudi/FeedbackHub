apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - migrate-job.yaml
  - api-deployment.yaml
  - worker-deployment.yaml

# secret.example.yaml is deliberately NOT listed: it holds fake values and
# exists only to document the key names (R-102). Create the real Secret out of
# band before applying.

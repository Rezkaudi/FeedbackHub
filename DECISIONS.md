# DECISIONS

The choices that mattered. Each one has four short parts:

- **Problem** — what we had to solve.
- **Options** — what else we could have picked.
- **We picked** — what we chose.
- **What we get** — what this buys us.

A decision that changes later gets a new number. The old one stays.

Rule numbers (R-nn) point into `references/SRS.pdf`.

---

## Security and sessions

### D-01 — Tokens live in cookies the browser cannot read
- **Problem** — Where to keep the access token and the refresh token after sign-in.
- **Options** — `localStorage`. A JavaScript variable. `HttpOnly` cookies. A session id with the tokens on the server.
- **We picked** — The server does the sign-in handshake. Both tokens go out as `HttpOnly` cookies. The browser never holds a token.
- **What we get** — No script on our page can read a token, so a cross-site scripting bug cannot steal a session. There is also no token code in the front end at all.

### D-02 — `SameSite` plus an `Origin` check
- **Problem** — Browsers send cookies on their own. Another site can make the browser act as our user.
- **Options** — `SameSite` alone. `SameSite` plus an `Origin` check. Both plus a third cookie the front end echoes back. The `csurf` package.
- **We picked** — `SameSite` plus an `Origin` check on the server. No GET ever changes data.
- **What we get** — About ten lines of code, nothing to store, nothing for the front end to remember. Both checks run on the server, so the UI cannot get them wrong.

### D-03 — Cookie names and lifetimes come from config
- **Problem** — They were written into the code, and the lifetimes must match the identity provider.
- **Options** — Leave them in the code. Move them to environment variables.
- **We picked** — Environment variables (R-3f).
- **What we get** — One value can change per environment with no rebuild, and the cookie lifetime can be kept in step with Keycloak.

---

## Reading data

### D-04 — Board paging uses `page` and `pageSize`
- **Problem** — The SRS set page sizes but never named the query parameters, so the front end and the back end had nothing to agree on.
- **Options** — `page`/`pageSize`. `page`/`per_page`. `offset`/`limit`. A cursor. Keep the 100-row maximum, raise it, or drop it.
- **We picked** — `page` and `pageSize`, with no maximum. R-21, R-22 and R-96 were changed to match.
- **What we get** — Two names that say what they mean, one shared contract for both sides, and the freedom to read the whole board in one call when a script needs it.

### D-05 — Comments are flat, newest first, read with a cursor
- **Problem** — R-33 said flat, oldest first, paged. On an old request the newest comment is the one people need, and oldest-first buries it on the last page.
- **Options** — Oldest first with pages. Oldest first opened at the last page. Newest first with pages. Newest first with a cursor. Ranked by relevance.
- **We picked** — Newest first, read with a cursor (`cursor` and `limit`) built from `created_at` and `id`.
- **What we get** — The newest comment is the first thing read, with no paging. A new comment appears at the top with no reload. A cursor never shows a comment twice and never skips one, however many arrive while a person is reading.

### D-11 — Search runs at read time
- **Problem** — A `search_vector` column and a `last_activity_at` column cost a write on every change.
- **Options** — Keep the tsvector and its trigger. Keep one of the two. Drop both.
- **We picked** — Drop both. Search is a plain `ILIKE` over the title and the description, run when we read.
- **What we get** — Two fewer columns, no trigger to keep correct, and no write amplification on every edit. Search still works. A trigram index can be added later without changing any data.

---

## Where settings live

### D-06 — Theme, sort and filters live in the browser. Language lives on the server.
- **Problem** — The brief asks where configuration lives, and how the front end gets it without a chain of blocking calls at startup.
- **Options** — All on the server. All in the browser. Split them by who needs them.
- **We picked** — Theme, default sort and default filters in `localStorage`. Language on the server, with a copy in the browser.
- **What we get** — The server only stores what it actually uses. The first paint never waits for a network call, and the app still draws in the right language when `/bootstrap` is slow or fails.

### D-07 — Email preferences are two boolean columns
- **Problem** — Two notification choices needed a home.
- **Options** — A preferences table keyed by user and event type. Two `NOT NULL DEFAULT true` columns.
- **We picked** — Two columns.
- **What we get** — No join on every read, no missing-row case to handle, and a default that is true for everyone from the moment the account exists.

### D-09 — `app_settings` keeps one real feature flag
- **Problem** — The settings row carried defaults that nothing read.
- **Options** — Keep the global-default ladder. Drop the fields nothing reads.
- **We picked** — Drop `default_theme`, `default_language`, `default_sort`, `version`, `updated_by` and `features`. Add `feature_comments_enabled`, which blocks the screen **and** the server (R-42).
- **What we get** — One flag that visibly changes the app when an admin toggles it, which is exactly what the brief asks for, instead of a bag of settings nobody reads.

---

## Fields we removed

### D-08 — No status-history table
- **Problem** — We had invented a table recording every status change. The brief never asks for one; "history" there always means Git history.
- **Options** — Keep it. Keep only the last change. Drop it.
- **We picked** — Drop it. Eleven tables became ten.
- **What we get** — One less table and one less write on every triage action, and the data model now matches the brief instead of our guess.

### D-10 — No terminal-status flag
- **Problem** — `statuses.is_terminal` existed to drive a "hide closed" filter we never specified.
- **Options** — Keep the flag and build the filter. Drop both.
- **We picked** — Drop the flag. R-50 is deleted.
- **What we get** — No half-built filter, and admins configure statuses as plain names with no hidden meaning attached to any of them.

### D-12 — No `moderated_by` column
- **Problem** — Nothing reads who approved, rejected or deleted a comment.
- **Options** — Keep the column. Build a real audit log. Drop it.
- **We picked** — Drop it.
- **What we get** — One less write path on every moderation action, and no column pretending to be an audit trail. If an audit trail is needed, it gets built properly.

### D-13 — `invitations` keeps only what is read, and R-66 is filled
- **Problem** — `invited_by` and `accepted_user_id` were never read.
- **Options** — Keep them. Drop them.
- **We picked** — Drop both. Fill the empty R-66: only an admin may create, list or revoke an invitation, checked on the server.
- **What we get** — Two fewer foreign keys, and a numbered rule with a test behind it for something part 4 only said in passing.

### D-14 — Categories and statuses read in `created_at` order
- **Problem** — `sort_order` meant building a drag-to-reorder screen and an endpoint.
- **Options** — Keep the column and build reordering. Keep it unused. Drop it.
- **We picked** — Drop it. Seed data is created in pipeline order, so the statuses read New, Under Review, Planned, In Progress, Done, Declined.
- **What we get** — The lists come out in the right order with no UI, no endpoint and no tests to write for them.

---

## Rate limits

### D-15 — Three sliding windows, changeable while the app runs
- **Problem** — The brief asks for rate limits on submissions. Nothing stopped a loop of writes.
- **Options** — Constants in the code. Environment variables. Rows in `app_settings`. A fixed window or a sliding one.
- **We picked** — Six fields in `app_settings`: sign-ups 20 per hour for the whole app, new requests 10 per hour per person, votes 100 per hour per person. Sliding window. The refusal names the time to try again (R-131). The count and the write happen in one database step (R-132). Deleted requests still count inside the window.
- **What we get** — An admin can retune a limit without a deploy. A sliding window means no burst at the top of the hour. One database step means two calls in the same second cannot both slip through. Counting deleted requests closes the write-delete-write loop.

---

## Stack and architecture

### D-16 — Angular 20, standalone components, signals, Tailwind
- **Problem** — The brief names Angular and asks us to justify the version, the state management, the styling and the component approach.
- **Options** — Angular 17 or 20. State: NgRx, NGXS, a `BehaviorSubject` service, or signals. Styling: Angular Material, PrimeNG, CSS-in-JS, or Tailwind with our own components.
- **We picked** — Angular 20, no NgModules, lazy routing per feature. State in signals, one small store service per feature. Tailwind driven by a single design-token file.
- **What we get** — Far less boilerplate than an NgModule app, and the whole state layer in a few lines. One token file is what makes every screen look the same (H-9). Owning our components means we control their keyboard and contrast behaviour instead of arguing with a kit.

### D-17 — Node.js 22 with NestJS 11
- **Problem** — The brief prefers Node. The framework is still ours to choose.
- **Options** — Express. Fastify. NestJS. A non-Node runtime.
- **We picked** — NestJS 11, TypeScript in `strict` mode.
- **What we get** — One agreed shape for modules, dependency injection and the guard chain, which is what H-9 and R-150 ask for. Dependency injection is what makes the ports-and-adapters split in D-18 cheap. The OpenAPI document falls out of the decorators (R-78).

### D-18 — A modular monolith, not a set of services
- **Problem** — The brief prefers a service split but says plainly that a good argument for simplicity beats an unjustified one.
- **Options** — One folder with no seams. One deployable with enforced module seams. Three or four services. A service per entity.
- **We picked** — One deployable cut into eight modules — `identity`, `requests`, `votes`, `comments`, `taxonomy`, `settings`, `invitations`, `notifications` — plus a separate worker for email. Inside each module: `domain`, `application`, `infrastructure`, `http`, with dependencies pointing inward. CI enforces both the module seams and the layer rule (R-143).
- **What we get** — The rules that matter here are transactional and touch two tables at once: count-and-write (R-132), one vote per person (R-26), one default status (R-47). A single database transaction gives us all three for free. No network hop to debug a vote. And the seams are real, so `notifications` can be lifted out later — which is why it already runs as its own process.

### D-19 — PostgreSQL 16, Prisma 6, raw SQL for the board
- **Problem** — The database and the way we reach it from TypeScript are ours to choose and justify.
- **Options** — Postgres, MySQL, MongoDB, SQLite. Prisma, TypeORM, MikroORM, Drizzle, or plain SQL.
- **We picked** — Postgres 16. Prisma owns the schema and the migrations. Repository interfaces live in `application` and their Prisma implementations in `infrastructure`, so no Prisma type reaches the domain or an HTTP response (R-147). The board query is one typed raw SQL statement (R-148).
- **What we get** — Every "the database stops it" promise is a real constraint: a unique index on `(request_id, user_id)`, a partial unique index for the single default status, foreign keys that refuse to drop a category still in use. Prisma's migration tool makes R-82 a real step instead of a script someone remembers to run. The board's search, filters, sort, pinned-first, paging and both derived counts come back in one round trip.

### D-20 — Redis 7 for shared state
- **Problem** — R-118 forbids the API keeping state in its own memory between calls, but counters, idempotency keys and the bootstrap cache need a home every copy of the API can see.
- **Options** — Process memory. Postgres. Redis.
- **We picked** — Redis 7, holding only the rate-limit counters, the idempotency keys and a short-lived bootstrap cache (R-149). Postgres stays the source of truth.
- **What we get** — Two API copies behind a load balancer share one counter instead of each counting to ten. Expiring counters are exactly the shape Redis is good at, and that write load stays off the table Postgres is guarding. Losing Redis degrades but never corrupts — one person gets one extra try.

### D-21 — Keycloak, self-hosted in this repository
- **Problem** — The brief requires an open-source identity provider with email/password and at least one social login, and forbids writing auth primitives ourselves.
- **Options** — Keycloak. Ory. Authentik. Zitadel. A hosted service with a free tier.
- **We picked** — Keycloak, with a seed realm exported as one JSON file and imported at startup, carrying the test accounts and the first admin.
- **What we get** — Email/password and social login work out of the box. OIDC is standard, so R-3a to R-3c are ordinary rather than clever. The realm import is what makes the one-command start honest: the reviewer runs everything from this repository, with no account to create anywhere.

### D-22 — Test-driven development in four layers, with Playwright on top
- **Problem** — The brief sets no coverage target, but part 4 of the SRS says every "no" needs a test proving the server says no.
- **Options** — Write tests after the code and chase a coverage percentage. Write only end-to-end tests. Drive the work test-first in deliberate layers.
- **We picked** — Red, green, refactor, for every numbered rule (R-156). Unit tests for the domain and use cases with fake ports. Integration tests for repositories and database constraints, against real Postgres and Redis via Testcontainers. API tests through the whole guard chain with Supertest. Front-end tests through what a person sees, with Angular Testing Library. Playwright end-to-end on the full compose stack, signing in for real through Keycloak (R-159, R-160).
- **What we get** — The rule numbers are the test list, so the tests read as the specification. Testcontainers is the only thing that can prove "the database stops it" — a mocked repository would pass with the constraint missing. Real sign-in in the end-to-end suite keeps the one thing that suite exists to prove.

---

## Building it

### D-23 — Testcontainers finds the container runtime by itself
- **Problem** — D-22 rests on Testcontainers, and Testcontainers assumes a Docker daemon. The machine this was built on runs podman emulating docker: its socket is not where Testcontainers probes, so every integration test failed at once with "Could not find a working container runtime strategy". Ryuk, the reaper Testcontainers starts to clean up, also cannot run from a rootless socket.
- **Options** — Write the two environment variables in the README and let each developer export them by hand. Commit them into an `.env.test` the test runner loads. Detect the runtime in a Jest setup file. Give up on Testcontainers and point the tests at a Postgres started by docker compose.
- **We picked** — Detect it. `test/support/container-runtime.ts` runs before the integration and API suites: if the developer has set `DOCKER_HOST` themselves it changes nothing, and otherwise, if a rootless podman socket exists, it points at that socket and turns Ryuk off. `npm run test:integration` works with no environment set up on either podman or real Docker.
- **What we get** — The tests run from a fresh clone with nothing to remember, which is the same promise R-80 makes for the app. The cost is real: turning Ryuk off means a crashed test run can leave containers behind, so containers are stopped explicitly in `afterAll` and a hard kill during a run needs a manual `podman rm`. The detection is also a guess about the machine — it reads a socket path built from the user id, and on an unusual setup it will guess wrong, though only ever when `DOCKER_HOST` was not already set. We did not drop Testcontainers, because a compose-managed database would make "the database stops it" depend on a service the test did not start.

### D-24 — The same rule is checked twice: in the DTO and in the database
- **Problem** — SRS part 14 says who stops each promise, and for several of them the answer is "Database". But the same rules also have to produce a helpful message under the right field (R-88), and a foreign-key error is not that message. So where does a rule like "a title is 5 to 120 letters" actually live?
- **Options** — Check it only in the DTO, and trust the code to run first. Check it only in the database, and translate the failure into a field message. Check it in both places.
- **We picked** — Both, with different jobs. The DTO check exists to give a good message and a 400; the database CHECK exists so the rule holds when two things happen in the same second, when a migration backfills, or when someone opens `psql`. The integration tests assert the *database* refuses, with the use case bypassed entirely, so a missing constraint fails the build even though the DTO would have caught it in normal use.
- **What we get** — The promise is as strong as the SRS says it is, and the error a person sees is still the useful one. The cost is duplication: the numbers 5 and 120 appear in a DTO and in a migration, and a change has to touch both — with a migration needed for the second. We accepted that because the alternative is a rule that only holds while the happy path is taken, which is exactly the failure R-115 was written to prevent. Where a rule needs *other rows* to check — "is this the last active category?" — there is no constraint to write, and the use case is the only guard; those are the cases where the SRS says "Server" rather than "Database".

### D-25 — The guard chain is composed at the root, not in the shared kernel
- **Problem** — The guard chain of R-138 is shared, so it looked like it belonged in the shared kernel with the error shape and the config. But its first link has to answer "who is signed in", and that answer comes from the `identity` module. The shared kernel may never depend on a module (R-141), so the obvious placement was actually a seam violation, and Nest refused to resolve it.
- **Options** — Let the shared kernel import `identity` and drop the seam. Put the whole guard chain inside `identity`. Keep the guard classes in the shared kernel and register them in `AppModule`, which is allowed to know about both.
- **We picked** — The third. `shared/http/` holds the guard classes and the `CurrentUserSource` port; `AppModule` binds the port to an implementation and registers the three guards in order.
- **What we get** — The seam survives, and the order of R-138 is still declared in exactly one place. The cost is that the composition root now carries wiring a reader might expect to find next to the guards, so both files carry a comment pointing at the other. While `identity` is unbuilt the port is bound to a placeholder that returns "nobody is signed in", which means every guarded route answers 401 — chosen deliberately, because an unfinished app that refuses everything is honest, where one that lets everything through is a hole.

### D-26 — The Keycloak realm pins the user ids the seed data expects
- **Problem** — Two systems hold the same three test people. Keycloak owns the account you sign in with; our `users` table owns the request they wrote and the vote they cast. They are tied together by `users.external_id`, which holds the Keycloak user id. On a fresh start Keycloak invents that id, so the seeded rows point at nobody: the reviewer signs in as `sam@feedbackhub.local` and sees an empty board, with a second Sam created beside the first.
- **Options** — Let the seed run after Keycloak and read the ids back over the admin API. Have the seed only create categories and statuses, and let the reviewer write the first request by hand. Match on email instead of id. Pin the ids in the realm export and use the same three constants in the seed.
- **We picked** — Pin them. `infra/keycloak/realm/feedbackhub-realm.json` gives each test account a fixed uuid (`1111…`, `2222…`, `3333…`), and `prisma/seed/seed.ts` writes those same values into `external_id`.
- **What we get** — The migration step of R-82 stays what it is: one job against Postgres, finished before the API starts, with no dependency on Keycloak being up and no admin credentials in the migration image. Signing in as a seeded account lands on the seeded person, so SRS part 15 is reachable on the first run. The cost is a hidden coupling — the same three uuids live in two files, and nothing fails loudly if one is changed alone; the symptom is an empty board, not an error. `infra/keycloak/realm/README.md` sits beside the export saying so. Matching on email was rejected because it would make an email change silently create a second account, and reading the ids back would have made the migration job wait on Keycloak.

### D-27 — The realm export carries no comments, so its notes live beside it
- **Problem** — The realm JSON encodes choices a reader needs explained: why the client is confidential, why PKCE is on, why the token lifetimes are what they are. The natural place is a comment in the file. JSON has none, so the usual trick is a `_comment` key.
- **Options** — `_comment` keys in the JSON. No explanation at all. A README beside the file.
- **We picked** — A README beside it. Keycloak 26 validates the import strictly and rejects any field it does not recognise, so a `_comment` key does not get ignored — it kills the import and the container exits 1, with the realm and all three test accounts missing. This was not theory: it happened on the first real run of the stack, and the only visible symptom downstream was that signing in did not work.
- **What we get** — The import is the thing the one-command start depends on, and it now cannot be broken by adding a note. The cost is that the explanation is one file away from what it explains, so a change to the lifetimes has two files to touch and nothing enforces the second.

### D-28 — Keycloak has two addresses, and the code asks it which to use
- **Problem** — Running the stack, sign-in died at the first redirect. The API reaches Keycloak at the container name `keycloak:8080`; a browser on the developer's machine can only reach it at `localhost:8080`. The code built the sign-in URL by joining the issuer it dials with `/protocol/openid-connect/auth`, so it sent the person to `http://keycloak:8080/…` — a host that does not exist outside the container network. Nothing appeared in any log, because from the server's point of view every step had succeeded.
- **Options** — Point the API at `localhost:8080` too (it cannot: inside a container that is the container itself). Ask the reviewer to add a line to `/etc/hosts` (then it is no longer one command, R-80). Add a second environment variable for the browser-facing address, and keep building the URL by hand. Configure Keycloak to publish both addresses, and take the URL from its discovery document.
- **We picked** — The last one. `KC_HOSTNAME=http://localhost:8080` makes the browser-facing address and the `iss` claim the public one; `KC_HOSTNAME_BACKCHANNEL_DYNAMIC=true` lets server-to-server calls answer on whatever name they arrived at. The discovery document then carries a `localhost` authorization endpoint and a `keycloak` token endpoint at the same time, and `startSignIn()` reads the URL from it instead of assembling one.
- **What we get** — One setting to be wrong instead of two that must agree, and the token's issuer matches what the library verifies against, so R-5 is unaffected. Sign-in works from a browser with nothing for the reviewer to configure. The costs are real: `startSignIn()` had to become asynchronous, because discovery is a network call, which changed the port, the controller and the test double; and the app now cannot build a sign-in URL at all when Keycloak is unreachable, where before it produced one that merely did not work. We prefer the honest failure — the readiness probe already reports the provider separately (R-83). A fourth option, a second environment variable, was rejected because two addresses that must be kept in step is exactly the kind of drift D-26 is already paying for elsewhere.

### D-29 — A `.env` file is read at start-up, and never wins over the real environment
- **Problem** — Nothing loaded a `.env` file. The app read `process.env` and nothing else, which is right for a container but means running the API from source needed roughly twenty `export` lines typed by hand, or a wrapper script. The failure was also confusing rather than loud: `prisma migrate deploy` worked, because the Prisma **CLI** reads `.env` by itself, and then the seed script failed with "Environment variable not found: DATABASE_URL" — the Prisma *client* does not.
- **Options** — Leave it, and document the exports. A shell script that sources a file before starting. `@nestjs/config`'s `ConfigModule.forRoot({ envFilePath })`. One `import 'dotenv/config'` at the top of each entry point.
- **We picked** — The last. `main.ts`, `worker.ts` and `prisma/seed/seed.ts` each load it as their first import; `dotenv` is now a declared dependency rather than one inherited from `@nestjs/config`.
- **What we get** — Running from source needs one file, and it is the same file the Prisma CLI already used, so there is one list of values instead of two. `dotenv` never overwrites a variable that is already set, so a container — which sets its own and ships no `.env` — behaves exactly as before, and the Zod check at boot is untouched: a missing value still stops the boot with every problem listed (R-102). `ConfigModule` was rejected because the app deliberately validates its environment with Zod in one place, and adding a second configuration system would be two ways to do one thing (R-150). The cost is that a stale `.env` is now a way to be confused — a value you thought you had changed in your shell is beaten by the file only if the shell variable is unset, and the file is invisible in `git status` because it is ignored. That is the usual bargain, and `.env.example` stays the committed list of what exists.

### D-30 — The API's CSP names `connect-src`, because it does serve one page

- **Problem** — `helmet` was configured with `defaultSrc: ["'none'"]` and a comment saying the API serves JSON and never a page. That stopped being true when R-78 put Swagger UI on `/api/docs`. Helmet ships no `connect-src` of its own, so `connect-src` inherited `default-src` and became `'none'`. The page itself loaded perfectly — `script-src` and `style-src` are named explicitly in helmet's defaults — so the docs looked fine until you pressed Execute, and every call died as "Failed to fetch". Swagger's own message blames CORS first, which is wrong here: the docs page and the API are the same origin, so CORS never enters it.
- **Options** — Drop the CSP on the whole API. Leave the global CSP alone and mount a second, looser one on the `/api/docs` route only. Serve the docs from somewhere else entirely. Add `connectSrc: ["'self'"]` to the one CSP.
- **We picked** — The last. One directive, added to the existing block.
- **What we get** — The docs work, and the loosening is exactly as wide as the problem: same-origin calls, nothing cross-origin, everything else still `'none'`. A CSP on a JSON response is inert anyway — it constrains a document, and there is no document — so the API surface gives up nothing real. The cost is that one CSP now serves two purposes, and the next thing added to `/api/docs` may need another directive with no test to catch it; nothing pins these headers today. The per-route override was rejected because it means two CSPs to keep in step, which is the drift D-26 is already paying for.

### D-31 — Healthchecks are written in `CMD-SHELL` form, not `CMD`

- **Problem** — The API container never once reported healthy. Its healthcheck was `test: ['CMD', 'node', '-e', "fetch(…)…"]`, which is correct Docker: the exec form runs the command directly, with no shell to confuse. `podman-compose` 1.0.6 does not implement it that way — it flattens the list into a single string and hands it to `/bin/sh -c`, which turned the command into `node' '-e' '…'` and produced `/bin/sh: syntax error: unexpected "("`. The Redis check next to it survived the same treatment, because `redis-cli ping` has no characters a shell cares about, which is what made the bug look specific to the API.
- **Options** — Require Docker and document podman as unsupported. Move the check into the image with a `HEALTHCHECK` line in the Dockerfile. Rewrite the probe with no shell metacharacters. Write the check in `CMD-SHELL` form with the quoting done once.
- **We picked** — `CMD-SHELL`. The Keycloak check three services above already used it, for the same reason.
- **What we get** — One form that behaves the same under Docker and podman, and the failure it was hiding is gone: `depends_on: service_healthy` on a container that can never turn healthy blocks anything waiting on it, so this was a start-up bug wearing a monitoring bug's clothes. The cost is that the command now goes through a shell it does not need, so its quoting matters and a stray quote is a runtime error rather than a parse error. A `HEALTHCHECK` in the Dockerfile was rejected because it moves the value out of the file where every other service's check is written.

### D-32 — The refresh cookie is scoped to `/v1/auth`, not to the refresh route alone

- **Problem** — The refresh cookie was written with `Path=/auth/refresh`, taken from `AUTH_COOKIE_REFRESH_PATH`. The app sets a global `v1` prefix, so the real route is `/v1/auth/refresh`. A browser matches `Path` against the whole URL path, so the cookie was sent to **nothing**: `POST /v1/auth/refresh` always answered 401, and `POST /v1/auth/sign-out` read `undefined` for the refresh token, skipped `endSession()`, and cleared our cookies while leaving the Keycloak session alive — so the next sign-in came back with no password asked. Nothing failed loudly. Sign-out returned 204 and looked correct.
- **Options** — Set the path to `/v1/auth/refresh` and leave sign-out unable to end the provider session. Set it to `/v1/auth/refresh` and have sign-out end the session with the access token instead. Drop the path scoping and use `Path=/` like the access cookie. Scope it to `/v1/auth`, which covers both routes that need it.
- **We picked** — The last. The default is now `/v1/auth`.
- **What we get** — Both routes that need the token receive it, R-9 works for the first time, and the scoping R-3e asks for is kept: the cookie still never reaches the board, the comments or anything else in the app, and it is still `SameSite=Strict`. The cost is that it is now sent to two more routes that do not need it, `/v1/auth/sign-in` and `/v1/auth/callback`, which is a real if small widening. Ending the session with the access token was rejected because `endSession()` revokes a refresh token, and rewriting it would have changed the provider port to fix a cookie bug. `Path=/` was rejected outright: that is the scoping R-3e exists to prevent. The prefix is now part of the value, which means this variable must change if the prefix ever does — the same drift D-26 warns about, accepted here because the alternative is building the path from the prefix at runtime and hiding the coupling instead of writing it down.

### D-33 — Keycloak sends its mail through Mailpit, set in the realm file

- **Problem** — "Forgot password?" ended on *"Failed to send email, please try again later."* `resetPasswordAllowed: true` only shows the link; Keycloak still needs a mail server, and `smtpServer` was absent from the realm import. Password reset had never worked.
- **Options** — Leave it, and document reset as unsupported. Tell the reviewer to fill in the Email tab in the admin console by hand (then it is no longer one command, R-80). Point Keycloak at a real SMTP server. Point it at the Mailpit container the compose file already runs.
- **We picked** — Mailpit, written into `feedbackhub-realm.json` as `host: mailpit, port: 1025`, no TLS and no auth.
- **What we get** — Reset works from a fresh clone with nothing to configure, and the mail is readable at `http://localhost:8025` next to the mail our own worker sends, so both are seen in one place. Nothing leaves the machine. The costs: the realm file now hard-codes a hostname that only exists inside the compose network, so a deployment that is not this compose file must override it; and the settings are the insecure ones, which are correct for a catcher and wrong for anything else. A real SMTP server was rejected because it needs credentials that cannot be committed (R-102).

### D-34 — Google is defined in the realm, disabled, and trusts the email it sends

- **Problem** — R-2 asks for at least one social sign-in. Only GitHub was defined, and its credentials cannot be committed, so in practice the repository shipped no social provider anyone could switch on quickly. Google is the one most reviewers already have an account with.
- **Options** — Leave GitHub as the only one. Add Google and commit real credentials (forbidden by R-102). Add Google and read the id and secret from environment variables. Add Google disabled, with placeholder credentials, exactly as GitHub already is.
- **We picked** — The last, so the two providers are configured the same way and there is one pattern to learn.
- **What we get** — Turning Google on is three fields in the admin console and no file to edit, and the realm file documents that the option exists rather than leaving the next person to discover it. `trustEmail` is `true` for Google and stays `false` for GitHub: Google verifies the address it hands over and GitHub may not, and the `domain_restricted` rule (R-67) refuses an unverified address even on an allowed domain — so the difference decides whether a social sign-in is let in at all. The costs: a disabled provider is dead configuration that no test covers and nobody has ever switched on, so it may be subtly wrong and we would not know; and `trustEmail` is a statement about a third party we cannot verify — if Google ever hands over an unverified address, R-67 is bypassed for that person. Environment variables were rejected because the realm import is a JSON file read once at first start, and adding a substitution step to it buys nothing when the admin console already edits the same setting live.

### D-35 — The `iss` on the callback is passed through, not dropped

- **Problem** — Every sign-in failed, with a password and through Google alike, and always the same way: the browser came back to `/v1/auth/callback` and was redirected to `/sign-in-problem?problem=sign_in_failed`. The API log said `RPError: iss missing from the response`. Keycloak 26 advertises `authorization_response_iss_parameter_supported: true` in its discovery document and puts `iss` on the callback (RFC 9207, which exists so a client with several providers cannot be tricked into sending a code to the wrong one). `openid-client` therefore *requires* that parameter. Our controller read `code` and `state` out of the query and built a fresh object from them, so `iss` was thrown away one line before the library looked for it. Nothing in the tests could see this: they replace the provider with a stub, so the only code that ever received an `iss` was the code that never ran.
- **Options** — Turn the check off in `openid-client`. Hand the raw Express request to the adapter and let it call `client.callbackParams()`. Rebuild the parameters in the adapter from a wider input. Add `iss` to the port's input and pass it through untouched.
- **We picked** — The last. `completeSignIn` takes an optional `receivedIssuer`, the controller fills it from `@Query('iss')`, and the adapter puts it back on the parameter object only when it is present.
- **What we get** — The check RFC 9207 asks for actually runs, and it runs where it belongs: the library compares `iss` against the issuer it discovered, so we are not writing an auth primitive of our own (R-1). The parameter is optional, so a provider that sends none still works. The costs: the port grew a field that is plainly OIDC vocabulary leaking through an interface meant to be provider-neutral, and a caller that forgets it gets a failure that says nothing about the caller. Handing over the raw request was rejected because it would put Express inside the port and let the adapter read anything it liked; turning the check off was rejected outright — it is a real protection, and it was the only thing that noticed we were wrong.

## The front end

### D-36 — Angular 22, and what that costs

- **Problem** — SRS §8.1 named Angular 20. The brief asks us to justify the version. Angular 22 shipped in June 2026 and is the current stable line.
- **Options** — Stay on 20 as written. Take 21. Take 22.
- **We picked** — 22, and this entry supersedes the SRS's 20 rather than pretending the SRS said it.
- **What we get** — Three things become free that we would otherwise have built by hand. `OnPush` is the default strategy in 22, so no component has to declare it and none can forget. Zoneless is the default, so there is no Zone.js to reason about. Signal Forms are stable, which matters more than it sounds: form rules live in a schema rather than a template, so every validator is unit-testable without rendering a component — exactly the TDD the assignment asks for. `httpResource` and `rxResource` are also stable, giving loading and error as signals for read paths. The costs, and they are real: 22 requires TypeScript 6.0.x and Node ^22.22.3, which is stricter than what this machine had (see the README); the ecosystem is younger, and `openapi-typescript` still declares a TypeScript `^5.x` peer, so it is run through `npx` with its own TypeScript rather than installed into the app; and the version is three months old, so there are fewer answers to search for when something breaks. Angular 21 was the safe pick and would have run on the machine as it stood, but its Signal Forms are experimental, and building every form in the app on an experimental API to avoid a Node patch upgrade is the worse trade.

### D-37 — A client-rendered SPA, not server-side rendering

- **Problem** — SSR is the default assumption for a new Angular app in 2026, and the tooling makes it a one-flag decision. It deserved to be decided, not defaulted.
- **Options** — SSR for everything. Hybrid: SSR some routes, CSR the rest. Prerender at build time. Client-rendered only.
- **We picked** — Client-rendered only, served as static files behind nginx, which proxies `/v1` to the API so the browser sees one origin (R-3h).
- **What we get** — SSR earns its cost when an anonymous visitor arrives cold from a search engine. This app has no anonymous route at all: the first thing anyone meets is a redirect to Keycloak, so there is no cold first paint to improve and no crawler to serve. Against that, three costs land squarely on us. `HttpOnly` cookies are not attached to `HttpClient` calls made on the server, and this app's browser holds nothing *but* `HttpOnly` cookies — so SSR would mean hand-writing credential forwarding for every request in the app, which is the one path we least want to hand-roll. Angular's server-rendering packages have carried four separate SSRF advisories, and the impact named in CVE-2026-27739 is session-cookie exfiltration; that class of bug exists only if SSR is running, and our whole auth model is session cookies. And SSR would make the theme flash worse, not better: the server cannot read `localStorage`, so it would render light and the browser would repaint dark, a flash that the client-only inline script does not have. The cost of choosing CSR is a slower first paint on a cold cache and no SEO — neither of which this product needs. If FeedbackHub were ever made public, this decision should be revisited, and hybrid rendering would be the shape to reach for.

### D-38 — Signals and small stores, not NgRx

- **Problem** — The app needs somewhere to keep the bootstrap snapshot, a board page, a comment cursor, and per-device preferences.
- **Options** — NgRx Store. `@ngrx/signals` SignalStore. Plain signals in one store service per feature.
- **We picked** — Plain signals, one store service per feature, provided on the route so it dies with the route.
- **What we get** — Most of what a state library would manage is not in a store at all: the board's search, filters, sort and page live in the URL, because R-22 requires a board view to be shareable and reachable with the back button. The store derives from the route rather than owning it. What is left is four small objects, and R-134 already prescribes this shape. The costs: no time-travel debugging, and the discipline that a library enforces structurally — one way to mutate, one way to select — is enforced here by review and by an ESLint boundary rule instead. If a cross-feature cache ever appears, `@ngrx/signals` is the migration and it is incremental.

### D-39 — Contract types are generated; the transport is ours

- **Problem** — The front end must not hand-copy the API's shapes into interfaces that silently rot.
- **Options** — `ng-openapi-gen` (full Angular client). `ng-openapi` (Angular-first, `httpResource`-aware). `@hey-api/openapi-ts` with its Angular client. `orval`. `openapi-typescript` for types plus a small typed wrapper over `HttpClient`.
- **We picked** — The last: `openapi-typescript` generates `schema.d.ts` from the live `/api/docs-json`, and a small `ApiClient` typed from it sits on `HttpClient`, with a thin gateway per feature.
- **What we get** — Every request keeps passing through Angular's interceptors, which is not negotiable here: the silent `401 → refresh → replay` path is the whole session model, and a client that bypasses `HttpClient` would bypass it. Drift protection is the same as any generated client — CI regenerates and fails on a diff. And the layer count is two rather than three, because we would still need gateways for signals and optimistic vote rollback on top of any generated service. The costs: roughly eighty lines of generic TypeScript that are ours to maintain and get wrong, covered by type-level tests; and `openapi-typescript` peers on TypeScript `^5.x` while Angular 22 wants `6.0.x`, so it runs via `npx` rather than as an installed dependency. The two Angular-native generators were rejected on maintenance, not on design: `ng-openapi-gen` last published in November 2025 and predates both Angular 21 and 22, and `ng-openapi` is at v0.3.2. Hey API's Angular client does wrap `HttpClient`, but its interceptor documentation is still marked under construction and the package is pre-1.0, which is not where the auth path should sit. At forty-three endpoints the hand-written wrapper is the smaller thing; at four hundred a full generator would win, and the migration is mechanical.

### D-40 — The app is colour-quiet because the colours are data

- **Problem** — Category and status colours are rows in the database that an admin edits at runtime (R-43, R-44). We cannot know them at design time, and we cannot know their contrast.
- **Options** — Use the admin's hex as a chip background with black or white text picked by a luminance calculation. Ignore the hex and use a fixed palette. Use the hex only as a small solid mark, with neutral text on a low-alpha wash of it.
- **We picked** — The last, and the app's own palette is kept deliberately quiet around it: warm neutrals, one blue that means "you can act on this", and semantic colour used only for state.
- **What we get** — Chip contrast is ours in both themes whatever the admin types, because the text is a neutral we control and never sits on an unknown colour. Every chip also writes its name next to the dot, so colour is never the only signal (R-111). And because the chrome is neutral, an admin's magenta category does not fight the interface on every row of the board. The costs: the admin's chosen colour is less prominent than they might expect — it is a dot and a wash, not a filled badge — and the wash needs a different alpha per theme, which is one more token to keep honest. Picking text colour by luminance was rejected because it is a coin-flip at mid-tones and produces chips that are technically passing and genuinely hard to read; every value in the palette was instead measured against its own background in both themes, which caught two failures that looked fine by eye.

### D-41 — IBM Plex Sans and IBM Plex Sans Arabic

- **Problem** — R-57 requires the language to switch at runtime between English and Arabic, so both scripts appear in the same product and often on the same screen.
- **Options** — A Latin font with an unrelated Arabic fallback. Two separately chosen families. One superfamily covering both scripts. The system stack.
- **We picked** — IBM Plex Sans with IBM Plex Sans Arabic, both on Google Fonts, with IBM Plex Mono for digits that must not shift.
- **What we get** — One superfamily drawn by one team means the same weights, proportions and rhythm in both scripts, so switching language changes the words and not the feel of the product. The costs: two font families to load rather than one, and Plex is a common choice, so the result reads as competent rather than distinctive. The design-system tool suggested Fira Code for headings; that was rejected — monospace headings make a feedback board read as a developer tool, and most of the people writing on this board are not developers. Fira also has no Arabic.

### D-42 — The end-to-end suite is its own package, with its own TypeScript

- **Problem** — Playwright has to live somewhere. The obvious home is `apps/web`, next to the front end it drives.
- **Options** — Put it in `apps/web`. Put it in `apps/api`. Give it a package of its own at `e2e/`.
- **We picked** — A package of its own.
- **What we get** — Angular 22 requires TypeScript 6 (D-36), and Playwright's peer range does not reach it; installed together, one of them is wrong. Separate packages let each have the version it supports, and the e2e package stays tiny — Playwright, axe and types, nothing else. It also makes the dependency direction obvious: the suite depends on the running system, not on either codebase, and cannot accidentally import a component and start testing internals. The costs: a third `npm install` for anybody running everything, and no shared types with the API — the seeded ids and the settings shape are written out by hand in `support/`, so a change to either is caught by a failing test rather than by the compiler.

### D-43 — Sign-in in the tests goes through the real Keycloak form

- **Problem** — Every end-to-end test needs a session first. Doing it properly costs a page load and about a second, on every test.
- **Options** — Mint a token and set the cookie directly. Sign in once and reuse the saved storage state for every test. Sign in through the real form in each test.
- **We picked** — The real form, every time (R-160).
- **What we get** — The one thing an end-to-end test can prove that no other layer can is that the round trip works: the guard redirects, the API starts code + PKCE with a confidential client, Keycloak authenticates, the API swaps the code on the server and sets HttpOnly cookies. A minted cookie tests none of it and would keep passing with the whole flow broken — which is exactly how D-32 survived a green pipeline. The cost is real: roughly a second per test, about a minute across the suite. Saved storage state was the tempting middle option and was rejected for the same reason as minting — after the first test it is a cookie from a file, not a sign-in, and the tests that check the cookie flags would be checking the file.

### D-44 — The suite lifts the submission rate limit while it runs

- **Problem** — R-130 allows ten requests per person per hour. The suite files two, so the fourth or fifth run within an hour starts failing tests that have nothing to do with limits, with a message about a missing link.
- **Options** — Leave it, and accept that the suite can only run five times an hour. Give each run a fresh database. Have the tests share one request instead of making their own. Lift the limit for the run and put it back.
- **We picked** — Lift it in the global setup, restore it in the teardown, and give the rule its own test.
- **What we get** — The suite runs as often as anybody wants, and R-130 is still proven — by `rate-limits.spec.ts`, which sets a limit of one over a one-minute window and watches the server refuse, check the code, and check the time it says to try again. A one-minute window is the part that matters: an hour-long one would count every request left behind by every earlier run. The costs: the suite writes to application settings before it starts, so a run killed part-way can leave the limit lifted (the teardown says so if it cannot restore it), and nineteen of the twenty tests are running against a configuration no real deployment has. A fresh database per run was the cleaner answer and was rejected on time — it means a compose teardown and a full Keycloak boot for every run, which is about ninety seconds added to a three-minute suite.

### D-45 — A `<select>` marks its chosen option, rather than binding its own value

- **Problem** — Two pickers in the app were built as `<select [value]="…">` with `@for`-generated options. Both showed the wrong thing: the board's sort control said "Newest first" while the board was sorted by most votes, and the admin's status picker said "New" on a request that was Done.
- **Options** — Keep `[value]` and set it after the options exist. Reach for `ngModel` and pull in `FormsModule`. Mark the chosen option with `[selected]`.
- **We picked** — `[selected]` on the option.
- **What we get** — The cause is ordering: the select's own `value` is written before an `@for` has created any children, the browser has nothing to match, and it quietly falls back to the first option. Marking the option cannot be early, because the mark and the option are the same thing. This is not cosmetic — an admin shown "New" on a request that is Done changes its status without meaning to. Both were found by the end-to-end suite on a real browser and neither had a unit test; both now have one that fails without the fix (R-161). The cost: the condition is repeated on every option instead of written once on the select.

### D-46 — Sign-out is a request the app makes, not a page the browser visits

- **Problem** — The Sign out button did `location.assign('/v1/auth/sign-out')`. That is a GET; the route is `@Post`. The person was shown the API's raw JSON 404, and because the handler never ran, their cookies were left in place — signed in, looking at a page that said "Not found". Nothing at any test layer called `signOut`, so nothing caught it.
- **Options** — Change the route to a GET that redirects, matching how `sign-in` works. Keep the POST and have the front end call it, then reload. Make it a real `<form method="post">`.
- **We picked** — Keep the POST. The app calls it, then does a full page load of `/`.
- **What we get** — A GET was the smaller change and the wrong one: sign-out ends a session, and the Origin check that stops another site causing that only guards writes (R-3g), so a GET sign-out is a URL any page could put in an `<img>` tag to log our people out. Keeping it a POST leaves it inside the same protection as every other write. The reload is deliberate too — it throws away every store in memory, so nothing of the last person survives into the next sign-in, and the guard takes it from there. The costs: signing out is now two round trips rather than one navigation, and the button needs a `void` wrapper because a template cannot await. The refresh interceptor already listed `/v1/auth/sign-out` among the paths it must never retry, which is a good sign this was the shape originally intended.

### D-47 — The provider's session is ended from our server, on the host our server can reach

- **Problem** — With D-46 in place, sign-out cleared our cookies and still did not sign anybody out. The guard redirected to Keycloak, Keycloak's own session cookie was still valid, and it answered with a fresh code without asking anything. The person pressed Sign out, watched the page reload, and arrived back on the board. R-9 says sign-out ends the session at the identity provider too, and `revoke` — what we were calling — retires one refresh token; it does not end a session.
- **Options** — Send the browser to the provider's end-session endpoint with an `id_token_hint`. Send it there with `client_id` and a post-logout redirect. Call the end-session endpoint from our server with the client's own credentials and the refresh token.
- **We picked** — The back-channel call, from our server.
- **What we get** — `id_token_hint` was rejected outright: it puts a token in a URL, and R-3c forbids that in as many words. The `client_id` variant avoids the token but makes the provider show its own "Do you want to log out?" confirmation, which is a screen we would be adding to our product without owning it. The back-channel call needs no redirect at all and keeps every token on our side, which is the same principle as the rest of the handshake (R-3a). One trap, and it cost a build to find: **Keycloak publishes a different host for front-channel and back-channel endpoints.** The token endpoint comes back as `keycloak:8080`, the end-session endpoint as `localhost:8080`, because Keycloak counts the latter as somewhere a browser goes. Posting to it from inside the container reached the container itself, the connection was refused, and the error was swallowed — a sign-out must not fail — so it looked exactly like the original bug. The path is now taken from the discovery document and only the origin replaced with the issuer URL we were configured with. The cost: one more place that knows the difference between the address a browser uses and the address we use, which is the same lesson D-35 taught about the authorization endpoint.

### D-48 — The end-to-end suite is Cypress, not Playwright

This supersedes the Playwright half of D-22 and D-42. The four-layer test
pyramid below Cypress is unchanged, and D-43 (sign in through the real Keycloak
form) still holds — it is now `cy.session()` per persona per spec.

- **Problem** — The `e2e/` package was scaffolded for Playwright but never filled in past a config file and a few stubs. It had to become a real suite.
- **Options** — Finish it in Playwright, as D-22 assumed. Rewrite it in Cypress. Rewrite it in WebdriverIO.
- **We picked** — Cypress, one browser (Electron), no retries, 15 spec files numbered by area, ~80 tests.
- **What we get** — `cy.origin()` handles the cross-origin Keycloak login as a first-class thing, and `cy.session()` caches a persona's cookies so each of admin/sam/rae signs in once per file rather than once per test — the cost D-43 worried about is now about six logins per run, not eighty. `cy.request()` shares the browser session, so the same test can drive the UI and then hit the API directly to prove the server enforces the same rule (R-70, R-3g). The costs: Cypress bundles its own Electron and will not start if the shell exports `ELECTRON_RUN_AS_NODE=1` (it reads the flag and runs as plain Node, then rejects its own `--smoke-test` arguments) — the README says to unset it; and `cy.request()` sends no `Origin` header of its own, so every write helper in `support/commands.ts` sets one explicitly or the OriginGuard answers 403. Playwright was not wrong, it was just never written; switching cost nothing already built.

### D-49 — Try again reloads the page once the start-up call succeeds

- **Problem** — When `/v1/bootstrap` fails, the app shows an error with a Try again button (SRS 15.8). The button called `bootstrap.load()` again, which did recover the store — but the router had already cancelled its first navigation when the guard saw a failed start-up, and a signal turning `ready` does not re-run a cancelled navigation. The person pressed Try again, the error vanished, and they were left looking at an empty page. Found by `11-errors-and-resilience`.
- **Options** — Have the guard return a redirect instead of `false` while the start-up is not ready. Re-trigger a navigation to the current URL from an effect once the store turns `ready`. Reload the page after a successful retry.
- **We picked** — Reload, from the button handler, only when `bootstrap.load()` left the store `ready`.
- **What we get** — A reload runs the whole start-up sequence again from a clean slate — the initializer, the guard, the first navigation — which is exactly the sequence that was interrupted, so there is no second code path to keep correct. The costs: a full page load rather than an in-place recovery, and the button handler now knows about `location`, which the component did not before. Re-navigating from an effect was the in-place answer and was rejected as more moving parts for a path taken only after a server error.

### D-50 — The whole presentation layer was rebuilt on Material Design 3, on request

- **Problem** — The front end asked to have its design and structure changed: Material Design 3, blue, a user menu, popups instead of pages, redesigned cards, redesigned admin. That is not a token swap on the old templates; it changes layout, interaction, and in places (new request, delete confirmation) the shape of the flow itself.
- **Options** — Reskin the existing templates in place, keeping their structure. Rebuild the presentation layer from the token file up, keeping the stores, guards, routes and API contract untouched.
- **We picked** — Rebuild, screen by screen, leaving `core/` and every `*.store.ts` alone except where a screen's redesign genuinely needed a new capability from one (the shared `VoteService`, `patchVote` on `BoardStore`).
- **What we get** — A design system that is actually one system — one set of tokens, one shared component kit under `shared/ui/`, used the same way on the board, the request page, settings and admin — rather than four screens each carrying their own idea of a button. The cost is size: every feature file changed, and the Cypress suite written against the old DOM was not re-run (see D-51). Reskinning in place was rejected because a popup instead of a page, or a confirm dialog instead of a typed-DELETE box, cannot be expressed as a CSS change.

### D-51 — The Cypress suite was not touched, and that is a known gap, not an oversight

- **Problem** — `e2e/` proves the old UI against a real browser and a real Keycloak. The redesign changes the header, the new-request flow, the filter controls' visual form, and every confirm-before-delete interaction. Updating fifteen spec files and re-running the whole stack was outside what this pass could do alongside the rebuild itself.
- **Options** — Update the specs alongside each screen's rebuild. Leave the suite exactly as it was and say so plainly. Delete the specs that will fail rather than leave them looking green when they are not run.
- **We picked** — Leave the suite as it was, run none of it, and say so in the README rather than claim a pass that did not happen.
- **What we get** — Nothing false is claimed. The ids and structure most selectors depend on (`#board-search`, `#board-sort`, the filter `<fieldset>`s, `<article>` cards) were kept on purpose during the rebuild specifically so most of the suite has a chance of still passing — but "has a chance" is not "was checked", and the header and new-request specs almost certainly need real changes, not luck. Updating specs alongside each screen was the thorough answer and was rejected on time; deleting them was rejected because a suite that used to prove real sign-in and real authorization should not be thrown away, only re-run.

### D-52 — The Material 3 blue palette is a hand-tuned tonal scale, not Google's exact HCT output

- **Problem** — M3 color roles come from an HCT (hue/chroma/tone) algorithm run against a seed colour. Reproducing that algorithm exactly, by hand, without the Material Theme Builder tool or a colour-science library, was not something to attempt from memory with any confidence.
- **Options** — Pull in `@material/material-color-utilities` to compute the real tonal palette at build time. Hand-write a 13-stop tonal scale (0/10/20/…/100) per role, built to the *structure* M3 asks for — light role pairs at tone 40/100, dark role pairs at tone 80/20, containers at 90/30 — using known-good published values for the M3 baseline blue seed (`#0B57D0`) as the anchor.
- **We picked** — Hand-written, structure-correct, built around the published baseline blue.
- **What we get** — Every M3 role (primary/secondary/tertiary/error/neutral × on-/-container, light and dark) exists as a real token in `styles/tokens/`, and the pairing rules that make M3 accessible by construction (a light on-color on a tone-≤40 surface, a dark one on a tone-≥90 surface) are followed throughout — so contrast is sound even though the exact hex values were not run through Google's algorithm. The cost: a value here or there may differ by a few percent lightness from what Theme Builder would output for the same seed; nobody has diffed them. Pulling in the utilities package was rejected to avoid a new runtime dependency for what is, in the end, a fixed set of constants.

### D-53 — Modals are the native `<dialog>` element, not a hand-built overlay or Angular CDK

- **Problem** — The redesign needs several modals: the new/edit request popup, the confirm dialog used everywhere something is destructive. Something has to own focus trapping, Escape-to-close, and a backdrop.
- **Options** — Bring in `@angular/cdk` for its overlay and dialog primitives. Hand-build a focus trap with a `div`, `tabindex` juggling and manual keyboard handling. Use the platform's own `<dialog>` element with `showModal()`.
- **We picked** — The native element, wrapped in one small `fh-dialog` component.
- **What we get** — `showModal()` gives real focus trapping and top-layer stacking for free, from the browser, not from application code that has to get it right. The cost is that jsdom (used by the Vitest specs) does not implement `showModal`/`close` at all, so `vitest-setup.ts` carries a small polyfill — `open` attribute toggling, a synthetic `cancel` event on Escape — that exists only to make the tests run and does nothing in a real browser, where the native behaviour takes over. Focus-return-to-trigger is also handled by the component itself rather than left to the browser, because the polyfill cannot provide it and a real, explicit behaviour is worth having regardless. CDK was rejected as a second design-system dependency sitting next to a hand-built token system that already disagrees with Material Design's own Angular components on several points (see D-16).
- **Later — the header band only shows when there is a title.** A dialog opened with no `heading` (the request form uses an `sr-only` `<h2>` via `labelledBy` instead) used to still render a full header row holding nothing but the close button, so the form started a `--fh-space-5` band below the top of the card for no reason. Now, with no heading, the close button floats in the top corner (`position: absolute`) and the body carries its own top padding. Options were: give every dialog a visible title (rejected — the request form's title is already the request's own `<h1>` right behind it, and a second "Edit request" heading is noise), or just shrink the empty band (rejected — still an empty band). Cost: a floating close button overlaps the top-inline-end corner of the body, so a headerless dialog must keep that corner clear (the request form's first field is a full-width label, which does).

### D-54 — Translation is a runtime dictionary lookup, not `@angular/localize`

- **Problem** — R-57 asks the whole interface to work in English and Arabic, switched by the person, without a reload.
- **Options** — `@angular/localize`, Angular's own i18n, which compiles a separate build per locale. A runtime `I18nStore` holding two typed dictionaries and a `| t` pipe.
- **We picked** — The runtime store and pipe.
- **What we get** — Switching language is instant — no reload, no separate bundle to fetch — because it is a signal flip, not a different build. The two dictionaries (`translations/en.ts`, `translations/ar.ts`) are typed against each other: `ar` must satisfy the exact key shape `en` defines, so a missing Arabic string is a compile error, and `dictionaries.spec.ts` checks the same thing at test time along with "no empty string in either dictionary." The cost: `@angular/localize`'s compile-time extraction and translator-friendly XLIFF workflow are given up in favour of two TypeScript files a developer edits directly — fine at this app's size, and a real cost if the string count or the number of languages grew a lot. `@angular/localize` was rejected specifically because per-locale builds cannot switch language without a reload, and R-57 asks for exactly that.

### D-55 — Deleting an account goes through the same confirm dialog as everything else, not a typed "DELETE"

- **Problem** — The account-deletion screen previously asked the person to type the word DELETE before the button would work — a stronger, bespoke pattern found nowhere else in the app.
- **Options** — Keep the typed-word pattern for this one screen. Move it onto the same `ConfirmService`/`fh-confirm-dialog` every other destructive action in the app now uses.
- **We picked** — The shared confirm dialog, with a danger tone.
- **What we get** — One pattern for "are you sure" across the whole app — delete a request, delete a comment, retire a category, delete an account — rather than a person having to learn a second, stricter ritual for exactly one screen. The message still says what is lost before the button is ever pressed (R-61), which is the part of the old design that mattered; typing a word out is friction without a matching increase in safety once a real confirm step already exists. The cost, honestly: typing DELETE is a slightly higher bar for an action this destructive, and an argument could be made either way — this was a judgement call in favour of consistency.

### D-56 — A request card carries its own vote button, sharing one `VoteService` with the request page

- **Problem** — The original board showed the vote count as text; voting only happened on the request page, specifically so the optimistic-update-and-rollback logic existed in one place (R-150). The redesign asks for a vote button on the card itself.
- **Options** — Duplicate the optimistic vote/rollback logic into the card. Give the card its own copy of `RequestDetailStore`. Extract the optimistic-vote mechanics into one injectable `VoteService` that both the card and `RequestDetailStore` call.
- **We picked** — `core/requests/vote.service.ts`, injected by both.
- **What we get** — The rule R-150 was protecting — one place that predicts the count, then replaces the prediction with the server's real answer, or puts it back on failure — is still one place, now reusable rather than page-only. `vote.service.spec.ts` covers the double-click guard, the rollback, and the 404 case once; `request-card.spec.ts` and `RequestDetailStore`'s existing spec each prove their own wiring on top of it without re-proving the mechanics. The cost: the card keeps a small local `override` signal so its own optimistic count is visible immediately even before a parent store re-renders it — a `linkedSignal`-shaped pattern repeated in a few other "seed from input, then edit locally" spots in the new code (profile form, account form, registration card).

### D-59 — Theme and language moved out of the header bar and into the user menu, as segmented buttons, not a dropdown

- **Problem** — Theme and language each lived in the header as a lone icon button that opened its own small dropdown menu — two extra controls competing with the main nav and the account menu for header space, and each pick took two clicks (open the dropdown, then choose). The header's main nav also duplicated the "Admin" link that already exists as a menu item inside the user menu.
- **Options** — Leave both as header icon-dropdowns. Turn them into always-visible segmented button groups directly in the header (three theme icons, two language codes, sitting loose next to the account chip). Move them inside the user menu, next to the account entry, as segmented button rows.
- **We picked** — Inside the user menu, as two rows (`Theme`, `Language`) each holding an `fh-segmented-control` — one click on `dark`, `light`, `system`, `EN` or `AR` applies it immediately, no submenu, no second click. The header's `main-nav` no longer repeats the Admin link, since it is already the second item in the user menu for an admin.
- **What we get** — One place for account-related choices instead of three separate header controls, and one fewer click per theme or language change. The new `fh-segmented-control` (`shared/ui/segmented-control/`) is a small generic `role="radiogroup"` of `role="radio"` buttons, reused as-is in `settings` (device preferences' theme picker, account form's language picker), so the same visual and interaction pattern appears in exactly one place in code. Cost: the header now carries only the account chip on the right, which is a narrower profile than before — a deliberate trade for fewer controls, not an oversight.

### D-60 — A generic `SegmentedOption.srLabel` carries the accessible name when the visible label is intentionally short

- **Problem** — The language segmented control shows `EN` / `AR` (so it fits as a compact row inside the user menu and the settings page), but a screen reader announcing "EN, radio button" is worse than announcing "English". The theme control has the opposite shape: icon-only buttons where the icon itself carries no name, so the label was already doing double duty as the accessible name.
- **Options** — Always show the full language name (`English` / `العربية`), accepting the wider row. Hardcode a separate `aria-label` per usage site, duplicating the "which language is this" logic outside the dictionary. Give `SegmentedOption` an optional `srLabel`, read once by `fh-segmented-control` for `aria-label`/`title`, falling back to `label` when absent.
- **We picked** — The `srLabel` field, sourced from the existing `header.languageEnglish` / `header.languageArabic` dictionary keys.
- **What we get** — The visible row stays compact everywhere it is used, while a screen reader still hears the real language name in either language; the theme control needs no change since its `label` already carries the right name. Cost: one more optional field on `SegmentedOption` for callers to remember when a visible label is abbreviated.

### D-61 — The user menu's text was on three different type scales; it is now one, with weight carrying the hierarchy

- **Problem** — A screenshot showed the user menu looking visually inconsistent: the account name used `title-small` (14px/600), the Theme/Language row labels used `label-large` (14px/600, coincidentally the same numbers), but `fh-menu-item` — Settings, Admin, Sign out — used `body-large` (16px/400). Three different M3 type roles doing three unrelated jobs in one small panel reads as mismatched fonts even though the family never changed.
- **Options** — Keep the M3 role names but pick ones that already matched (harder than it sounds — no single pair of existing roles covers "name", "row label" and "item" at one size). Introduce a menu-local scale, hardcoded once, so every piece of text in this panel sits at 14px and only `font-weight` marks what is a heading versus a plain row: **600** for the account name, **500** for the Theme/Language labels, **400** for Settings/Admin/Sign out, keeping the small badge (11px) genuinely a badge, not body copy.
- **We picked** — The second option, applied directly in `user-menu.css` and, since `fh-menu-item` is only used inside this menu, in `menu-item.css` and `segmented-control.css` too.
- **What we get** — One visual rhythm for the whole panel; hierarchy is legible through weight and color (on-surface vs on-surface-variant) rather than through the reader having to parse three unrelated sizes. Alongside this, the panel picked up a visually distinct "preferences" surface — Theme and Language now sit inside their own bordered, tinted card instead of floating as plain rows against the same background as the header and the action list — and the header avatar got a thin primary-colored ring for a touch of identity emphasis. Cost: `fh-menu-item`'s type scale is now hardcoded rather than borrowed from the shared type tokens; acceptable while it has exactly one consumer (the account menu), worth revisiting with a menu-specific type token pair if a second menu ever reuses it at a different size.

### D-62 — The board toolbar's sort control became a custom pill, matching the search field and the Filters/New request buttons, instead of a plain native `<select>`

- **Problem** — A screenshot showed the toolbar row with three different shapes side by side: the search field a full pill, the Filters and New request buttons full pills, and the sort dropdown a sharp-cornered, unstyled native `<select>` with the browser's own arrow — one visibly different control in a row that should read as one family of controls.
- **Options** — Reuse the shared `.fh-select` class (used by every form field elsewhere in the app) — but that class is deliberately `corner-small` for form-field contexts, and using it here would just trade one mismatch (sharp vs pill) for another (still not a pill, and now inconsistent with the fields it was designed to match). Replace the native `<select>` with a button that opens an `fh-menu` of options, matching Filters visually exactly — rejected: a `<select>` keeps native keyboard support, mobile picker UI and the existing `#board-sort` Cypress selector for free, and none of that is worth trading away for a cosmetic match. Keep the native `<select>`, but restyle it: `appearance: none`, a leading sort icon and a trailing custom chevron absolutely positioned over it (the same technique `board-search` already uses for its search icon), `corner-full`, and the same outlined-pill visual treatment as the Filters button.
- **We picked** — The restyled native `<select>`, in its own `board-sort.css` rather than sharing `.fh-select`. Needed one new icon (`arrow-up-down`) added to the shared icon registry, since there wasn't a sort glyph yet.
- **What we get** — The whole toolbar row now reads as one row of pill-shaped controls (search, sort, filters, new request), while `#board-sort` is still a real `<select id="board-sort">` — the Cypress selector, native keyboard behavior and mobile picker are all untouched. Cost: a second visual convention for form-like controls now exists in the codebase (`.fh-select`'s `corner-small` for form fields, this pill treatment for the toolbar) — deliberate, since a toolbar filter control and a form field are different contexts.
- **Responsive** — The toolbar row already wrapped (`flex-wrap: wrap`), but had no rule making that wrap look intentional at narrow widths — search, sort, filters and the new-request button could each land on their own line in an arbitrary order. Added one rule: under 640px, the search field takes the full row on its own (`flex-basis: 100%`, moved first with `order: -1`), with sort, filters and new request wrapping together beneath it at their natural size. Considered stretching all three into an equal-width row to fill the second line exactly — rejected for now: `fh-button`'s host has no `width: 100%` contract, and forcing one on from a parent stylesheet through Angular's view encapsulation would only reach the host element, not the button element inside it, leaving a stretched host with a content-sized button adrift inside it. Written here rather than done partially: a `fill`/`block` input on `fh-button` would be the proper way to offer that later.

### D-63 — The Filters panel became a real dialog, everywhere, instead of a hand-anchored popover

- **Problem** — A phone screenshot of the D-62 popover showed the real cost of the anchored-`position: absolute` approach: on a narrow screen the panel floated over the request cards with no backdrop dimming it from the page behind it, its width came from `inset-inline-start/end: 0` resolved against `.filters-group`'s own shrink-to-fit box (not the viewport), and each status/category option rendered as one full-width chip per row, making the list tall enough to need real scrolling inside a box that itself had no scroll handling. D-57 had already flagged, honestly, that this popover never got focus-trapping.
- **Options** — Patch the popover further: give it a real backdrop `<div>`, recompute its width for mobile with JS instead of CSS, add manual focus-trapping. Branch the template so mobile renders the filters in `fh-dialog` while desktop keeps the popover, doubling the markup and the states to keep in sync. Replace the popover outright with the shared `fh-dialog` — the same native `<dialog>`-based component already used for New Request, confirmations, and Settings' danger zone — at every viewport width.
- **We picked** — Replace it outright with `fh-dialog`.
- **What we get** — One `<dialog>` gives, for free, everything the popover was missing: a real `::backdrop` dimming the page (the "floating over cards" problem is gone because there is no longer a page behind it to float over), native centering that does not depend on `.filters-group`'s box size, a built-in scrollable body capped at `85vh` so a long status/category list scrolls inside the dialog instead of running off the bottom of the screen, Escape-to-close and backdrop-click-to-close without a hand-rolled `ClickOutside` directive or a `keydown.escape` handler, and — closing the D-57 gap outright — real focus trapping, because that is what `showModal()` does. The board toolbar's own CSS lost the entire `.filters-panel*` block (anchoring, width, the mobile override, the open/close animation) in exchange for reusing a component the app already had. Cost: the filters dialog is now visually a centered card rather than a dropdown anchored to the Filters button — a deliberate trade, since the anchored version is exactly what produced the mobile bug.

### D-64 — Active-filter chips moved into a dedicated stylesheet, the Filters count badge stopped using the error color, and every button gets a pointer cursor from one global rule

- **Problem** — Three smaller things, seen together on a phone screenshot of the finished toolbar: `active-filters.html` was the one board component still built from inline Tailwind utility classes on raw `<button>` elements instead of the app's `var(--md-sys-color-*)` token pattern every other component uses, so its chips and its "Clear all" link did not visually belong to the same system as the rest of the toolbar; the Filters button's active-count badge used `--md-sys-color-error` (red), which reads as "something is wrong" for what is just a neutral "6 selected" count; and "Clear all", like every other native `<button>` in the app, had no `cursor: pointer` — browsers do not apply that by default to `<button>`, only to links.
- **Options for the cursor** — Add `cursor: pointer` to each button-like component's stylesheet one at a time as they get noticed (already happening piecemeal — `fh-button`, `filter-chip`, `fh-menu-item` all separately declare it). Add one rule, once, in `base.css`, covering every native `<button>`/`<select>` and every custom interactive role (`menuitem`, `radio`, `tab`, `button`) app-wide, with a matching `not-allowed` rule for the disabled state.
- **We picked** — The one global rule in `base.css`. The existing per-component `cursor: pointer` declarations were left in place rather than stripped out — harmless duplication, and each one still documents that its element is meant to be clickable.
- **What we get** — Every current button gets a pointer cursor, including `active-filters`' "Clear all", and every future button gets one automatically without a component author having to remember to add it. `active-filters.html`/`.css` was rewritten to match the rest of the app: chips use `secondary-container` (the same "selected" token `filter-chip` and `fh-menu-item[selected]` already use) with a `currentColor` state-layer on hover instead of Tailwind's `bg-secondary-container`, and "Clear all" is now a real pill-shaped text button (hover fill, no underline) rather than an underlined link — consistent with the identical "Clear filters" control already inside the Filters dialog. The count badge moved from `--md-sys-color-error` to `--md-sys-color-primary`, keeping red reserved for things that are actually wrong. Cost: none identified — this was cleanup, not a trade-off.

### D-65 — Delete-a-request came back, on both the card and the detail page; it had quietly gone missing in the redesign

- **Problem** — Asked for a delete button on the board card, a check of the redesigned app turned up something the request itself hadn't mentioned: **there was no way to delete a request anywhere in the new UI**, not on the card, not on the detail page. The backend still exposes `DELETE /v1/requests/{id}` ("delete my own request, or any if I am an admin", R-14), the dictionaries still carry `snackbar.requestDeleted` and a whole `requestForm.deleteRequestTitle/Message/Confirm/Cancel` set of strings, and README still lists delete-a-request among the confirm-gated destructive actions — the capability, the copy and the docs all assumed it existed. It did not survive the rebuild. This was a real regression, not a design-taste question, and is fixed here rather than only noted.
- **Options** — Leave the detail page without it and add only the card button, since that is what was asked. Add a shared `RequestsService` used by both the card and the detail page. Add a `deleteRequest()` method directly on each of `BoardStore` and `RequestDetailStore` — the two places that already own the relevant list/row state — and wire a delete control into both `RequestCard` and `RequestHeader`.
- **We picked** — The third option. `BoardStore.deleteRequest(id)` calls the endpoint, then removes the row and decrements the total on success; `RequestDetailStore.deleteRequest()` calls it for the currently loaded row. No shared service, because unlike voting there is no optimistic-then-reconcile logic to keep in one place — it is one HTTP call and a local removal, small enough to not be worth a third layer.
- **What we get** — `request().isMine || bootstrap.isAdmin()` (the exact rule the schema's own doc comment on `isMine` names) gates a trash icon-button on the card, and the same rule gates one next to the existing Edit link on the detail page's header, both behind the app's one `ConfirmService` dialog (`requestForm.deleteRequestTitle`, quoting the title back, `requestForm.deleteRequestMessage` naming what is lost, tone `danger`). A successful delete on the board removes the row immediately from `BoardStore`'s signal — no full reload — and only re-fetches if that emptied the current page, so the empty-state messaging still appears correctly. A successful delete on the detail page shows the snackbar and navigates back to the board. `request-card.spec.ts` gained five new tests: no button for someone else's request as a non-admin, a button for my own, a button for an admin on anyone's, confirm-then-delete, and cancel-leaves-it-alone. Cost: none — this closes a gap, it does not trade one thing for another.

### D-66 — The card's second pass: pin for admins, every control in one footer row, status/category labelled, and the pin badge redesigned

- **Problem** — Four separate complaints about the same card, all from using it: (1) pinning only existed on the detail page's admin bar, not on the board where most browsing happens; (2) the delete button sat up in the header while voting stayed in the footer — two different places to look for "things I can do to this card"; (3) the status and category chips were two unlabelled coloured pills with no way to tell which was which without already knowing the taxonomy by sight — a real clarity problem, not a taste one; (4) the pinned-card treatment was a 3px coloured bar glued to the top edge of the card, which read as a stray line rather than a deliberate design choice, and the pin badge itself was a rotated icon that looked tilted rather than intentional.
- **Options for pinning** — Leave pinning on the detail page only (a click through to a second screen to pin something visible right on the board). Add a pin toggle to the card. Reuse `RequestDetailStore`'s pin logic somehow from the card (that store only knows about one loaded request, not a board row).
- **We picked** — `BoardStore.setPinned(id, pinned)`, calling the same `PATCH /v1/requests/{id}/pin` and writing the server's full returned row back into the board's rows — mirroring `RequestDetailStore.adminChange`'s existing "take the server's answer, do not hand-patch a field" rule, for the same reason: the response also carries recounted fields.
- **Options for one control location** — Keep delete in the header, only move pin there too (two different corners of the card still). Move every action — pin, delete, vote — into the footer, next to the author/date/comment-count row that was already there.
- **We picked** — The footer, as two stacked rows: the existing author/date/comments row on top, a new actions row underneath holding the pin toggle and delete icon-buttons (admin/owner-only, start-aligned) and the vote pill (everyone, pushed to the end — it is still the one action every visitor has). The header shrank back down to just the title.
- **Options for status/category clarity** — Add a leading icon per chip (a flag for status, a tag for category) relying on shape to carry the meaning. Add visible text captions. Do nothing and rely on people learning the taxonomy.
- **We picked** — Visible captions: a small uppercase "Status" / "Category" label sits above each chip. `shared/ui/chip/taxonomy-chip.ts` itself was not touched — it is reused elsewhere (admin taxonomy tables, the request detail header) where a caption would not make sense, so the label lives in `request-card.html`, wrapping each chip in its own labelled group, opt-in per call site rather than a change to the shared component's default look.
- **Pin badge and pinned-card treatment** — Dropped the top accent bar entirely; a pinned card is now marked only by its border turning primary-colored plus the corner badge, which itself moved from a rotated `corner-medium` square (the tilt read as a mistake, not a marker) to an upright circular badge, sitting half outside the card's top corner with a `surface-container-low`-colored ring separating it from the border underneath — a standard "affixed badge" treatment, and the pin icon itself now renders at its natural orientation instead of rotated 45°.
- **What we get** — Pinning is one click from the board, in the same place as every other action on the card; everything the card lets you do lives in one row at the bottom; a person who has never opened the admin taxonomy screens can now tell, without guessing, which chip is the state of the request and which is its subject. `request-card.spec.ts` gained three pin tests (hidden for a non-admin, pin, unpin). Cost: the footer is now two rows instead of one, so the card is a little taller — accepted, since the alternative was cramming pin, delete, vote and four pieces of author metadata onto one line.

### D-67 — The pin icon became a thumbtack, Pin and Vote carry visible text at desktop widths, and the status/category labels moved onto one line

- **Problem** — Three more things about the same card, from continued use: the shared `pin` glyph (D-66's redesign) was a filled map-pin teardrop — recognizable as "location," not "pinned to the top," and not what was actually asked for; the Pin and Vote controls were icon-only, so a first-time visitor had no way to know what either button did without hovering for a tooltip; and the Status/Category captions from D-66, while now labelled, were stacked as two two-line blocks side by side, using more vertical space than the meta really needed.
- **Pin icon** — Replaced the icon registry's `pin` glyph outright with a small filled circle plus a diagonal needle line — a minimal thumbtack, matching the reference image's read (a pin head with a point, not a map marker). Because `IconName: 'pin'` is one shared glyph, this fixes it everywhere the app uses a pin — the card's corner badge, the card's own toggle button, and the request detail page's existing pin/unpin control in `RequestAdminBar` — from one change.
- **Options for visible labels** — Add a `label` input to the shared `fh-icon-button` (used for Pin previously) so it could show text. Rebuild Pin as a custom `<button>`, matching the hand-built `vote-button` markup that already existed rather than going through `fh-icon-button` at all, and add a text span to both.
- **We picked** — The custom-button route for both, unified under one `.action-button` class so Pin and Vote share one visual language (outlined pill at rest, filled when active/voted) instead of Pin looking like a plain icon-button next to a differently-styled vote pill. Each carries a `.action-label` span holding "Pin"/"Unpin"/"Vote" — new short strings distinct from the existing full "Pin to the top" phrase, which stays as the `aria-label` so screen readers still hear the complete sentence regardless of what is visible. The label is hidden below `64rem` (the same width the board grid drops to fewer than three columns at) and shown above it — "desktop" defined the same way the rest of the toolbar already defines it, not a new threshold.
- **Status/category placement** — Went from two stacked (label-above-chip) columns to one inline row: each label now sits beside its chip on the same line, with a colon appended by CSS (`.meta-label::after { content: ':' }`) rather than baked into the translated string, so the punctuation does not have to be re-decided per language.
- **What we get** — One thumbtack icon used consistently everywhere a pin appears; Pin and Vote are self-explanatory by their visible words on any screen wide enough to read them, and still fully labelled for a screen reader on a phone where the words are hidden; the meta line is one row instead of two, shortening the card without losing the labelling D-66 added. `request-card.spec.ts`'s existing pin and vote assertions needed no changes — they query by accessible name (`aria-label`), which did not change, only the visible content around it did. Cost: none identified.

### D-68 — The pin icon's shape came from Google's own Material Icons source, not a hand-drawn approximation, and pinning got its own red, apart from primary blue

- **Problem** — Handed a reference image of a red pushpin and told the D-67 icon was still wrong, the honest read was that hand-drawing icon glyphs by eye (as D-67, and the original D-66 before it, both did) had twice produced a shape that did not read as a pin the way an off-the-shelf, purpose-made glyph does. Rather than attempt a third freehand guess, this pass fetched the actual path data for Material Icons' `push_pin` glyph from `unpkg.com/@material-icons/svg` — the same icon Google ships for exactly this "pin to the top" action — and used it verbatim. Separately, the pinned badge, the card's pin toggle and the pinned card's border were all still blue (`--md-sys-color-primary`), the same color as "voted" and every other selected/active state in the app, which is not what a red reference image was asking for.
- **Icon source** — `unpkg.com/@material-icons/svg/svg/push_pin/baseline.svg`, `fill-rule="evenodd"`, verified by fetching and reading the raw SVG rather than reproduced from memory. This keeps the "no icon font, no network at runtime" rule from D-52 intact — the path is copied into `icon.html` at build time like every other glyph in the registry; nothing is fetched by the shipped app.
- **Options for the color** — Reuse `--md-sys-color-error` (the app's only existing red) for pinning. Leave pinning blue and treat "the user wants red" as a one-off preference to override with a literal color inline. Add a dedicated `--fh-pin-accent` token, separate from `error`, so pinning has its own identity without overloading the color the rest of the app reserves for something going wrong (a rule D-64 had only just established for the filter-count badge).
- **We picked** — `--fh-pin-accent` / `--fh-on-pin-accent`, defined once in `color-roles.css` alongside the other hand-set literals that file already carries (`--fh-scrim`, `--fh-chip-dot-ring`), with a lighter tint for dark mode rather than the same hex reused unchanged — consistent with D-52's "dark mode is a role remap, not an inversion" rule applied to every other color in the system.
- **What we get** — A verified, correctly-shaped pin glyph used everywhere the app shows one, and a self-contained red identity for "pinned" — the badge, the card's toggle button (both at rest, on hover, and pressed), and the pinned card's border — that cannot be confused with an error state and did not require touching `--md-sys-color-error` at all. Cost: one more hand-set color pair to keep in sync across the three light/dark blocks in `color-roles.css`, same maintenance shape as the tokens already there.

### D-69 — The card's timestamp became relative, and the comment count got the same explicit labelling D-66 gave status and category

- **Problem** — Told plainly that the card's overall design still was not landing, and to reconsider comments and time rather than repeat the last pass: the date on every card was an absolute, fairly small date string requiring the reader to do the "how long ago was that" arithmetic themselves — and `RelativeTimePipe` already existed in `core/i18n/`, fully built and tested, and was not used anywhere in the app. The comment count, meanwhile, was a bare icon and number with no accessible name at all — the exact "which chip is this" problem D-66 had just fixed for status and category, still present one row down for comments.
- **We picked** — Swapped the card's date to `| relativeTime` ("3 days ago"), keeping the exact timestamp one hover away via a `title` attribute built from the existing `localizedDate` pipe rather than throwing the precision away. Gave the comment count an `aria-label`/`title` built the same way `voteLabel` already was (`{count} {comment|comments}`), and — matching the desktop-label treatment D-67 gave Pin and Vote — a visible "comment(s)" word that appears at the same `64rem` width, so a wide screen spells it out and a narrow one keeps the compact icon-plus-number.
- **What we get** — `RelativeTimePipe` finally has a caller, one less finished-but-unused file in the codebase; every count on the card (votes, comments) and both taxonomy chips now carry a real accessible name and, at desktop width, real visible words, so nothing on the card requires the reader to already know what an icon means. Cost: none identified.

### D-70 — The card's layout followed the category leader's own pattern: vote rail on the left, everything else in a column to its right

- **Problem** — Told the placement of status, category, pin, delete and vote was still wrong and to stop guessing and look at how this exact category of product does it, `feedback.canny.io` — the reference feedback-board product — was fetched directly. Its own card layout puts the vote count/button on the **left** of every row, with the title and everything else flowing to its right; that placement is not particular to Canny — Product Hunt, Reddit and Hacker News all put voting on the left edge for the same reason: it is the one action nearly every visitor takes, so it gets a fixed, thumb-reachable position that never moves regardless of how long the title or description runs. Two smaller things rode along: the loading skeleton still used the pre-D-70 `auto-fill(18rem)` grid, so it could show four placeholder columns on a wide screen while the real grid (D-66) caps at three; and the "no red for the pin button" ask (color stays for the badge, not the toggle) needed a second look at how the toggle got its red in the first place.
- **We picked** — Adopted the same left-rail pattern: `.vote-rail` is now a full-height button pinned to the card's left edge (icon, count, and the "Vote" word stacked vertically, always visible — there is room for it now, so the D-67 desktop-only label rule no longer applies to it) with the rest of the card — title, an owner-actions cluster, the status/category meta line, the excerpt, and the info row — in a column beside it. Pin and Delete moved out of the footer and into a small corner cluster next to the title, the conventional spot for a card's secondary/owner-only actions (an options-menu-in-the-corner is the same idea GitHub's issue cards and Trello's card-back menu both use) — freeing the bottom of the card to be exactly what was asked: name, relative time and comment count, with nothing below them.
- **Pin button color, actually fixed this time** — Re-reading the CSS while making this change found the D-66/D-68 "red on hover" rule for Delete, and the equivalent rule for Pin, never worked: both were written as `.request-card-delete:hover { color: ... }` targising a class Angular puts on `<fh-icon-button>`'s **host** element, while the color that is actually rendered is set explicitly, non-inherited, on the `<button>` *inside* `fh-icon-button`'s own encapsulated template — a parent-stylesheet class on the host cannot reach past that. Fixed by using the mechanism `fh-icon-button` already exposes for exactly this: `variant="danger"` for Delete (which does correctly render its icon in `--md-sys-color-error`), and no variant override at all for Pin, which already turns the M3-correct `primary-container` tonal color when `pressed` is true — never red, satisfying the request directly, and only the corner badge (D-68) keeps `--fh-pin-accent`.
- **What we get** — A card layout borrowed from the products this app is most directly comparable to, not invented from scratch a fifth time; a loading skeleton that matches the real grid's column count; and two icon-button color rules that now actually do something, where before they silently did nothing. `request-card.spec.ts` needed no changes — every assertion queries by accessible name or role, none of which moved. Cost: the vote button, no longer squeezed into a compact pill, is now a fixed-width rail that every card reserves space for regardless of content — accepted, since that fixed reachable position is the entire point of the pattern.

### D-71 — The vote rail shrank: no longer stretched to the card's full height, and the "Vote" word came back off

- **Problem** — A screenshot of D-70's rail on a real desktop grid showed the actual cost of `align-items: stretch`: on a short card ("0 Vote", one line of description) the rail stretched to match a neighbor's taller card, leaving a tall empty button with two words of text floating in a lot of unused space — exactly the "too big" complaint.
- **We picked** — `.request-card`'s cross-axis alignment changed from `stretch` to `flex-start`, so the rail now sizes to its own content (icon + count) instead of being forced to match whichever card in the row happens to be tallest. Width came down from `3.75rem` to `2.75rem`, padding tightened, and the "Vote" word was dropped rather than hidden — icon-plus-number, with no visible word, is the vote widget every reference product checked for D-70 (Reddit, Hacker News, Product Hunt) actually ships; the D-67 "show the word on desktop" reasoning does not hold once the word is what was making the desktop view too big in the first place. The full sentence ("7 votes. Vote for this request.") stays as the `aria-label`, so nothing was lost for a screen reader — only the visible word.
- **What we get** — A compact vote control sized to its own content on every card, not stretched to the tallest neighbor in its row; one less translation key (`requestCard.voteAction`) now that nothing renders it. Cost: none identified.

### D-72 — The left-hand vote rail from D-70 is gone; voting is back in the footer

- **Problem** — Told directly, after seeing it rendered, that the left rail was simply not wanted in this app — not a sizing complaint (D-71 had already fixed that), a placement one. D-70's reasoning (Canny, Product Hunt, Reddit and Hacker News all park voting on the left) was sound for those products, but it is not a universal law, and the person who owns this app gets the final say over a pattern borrowed from elsewhere when it does not fit how they want their own product to feel.
- **We picked** — Voting moved back into the footer info row, at its end (`margin-inline-start: auto` now lives on `.vote-pill` rather than on `.request-card-comments`, since vote is once again the last element, not comments) — a small pill matching the D-71 icon-plus-number shape, sitting beside the author, relative time and comment count rather than occupying its own column. `.vote-rail`/`.vote-rail-count` and the now-pointless `.request-card-body` wrapper div (it existed only to sit beside the rail) were removed outright rather than left dormant.
- **What we get** — One footer row carrying every piece of "about this request" information plus the one action everyone can take, title unobstructed by a permanent left column, and a card layout that is simpler than D-70's, not just reverted to it — `request-card-body` is gone for good, its only job was existing next to a rail that no longer exists. `request-card.spec.ts` needed no changes. Cost: none identified; this is a straightforward reversal of one specific placement choice, not a compromise.

### D-57 — Filters open as a popover card under the Filters button, the pin marker drops the full-card wash, and the user menu header carries a role badge

- **Problem** — After the first M3 pass shipped, a look at the running app showed three spots that were still weak: the filter panel was two bare `<fieldset>`s sitting loose in the page flow with no surface of their own; a pinned card turned its whole background into the primary-container tint, which reads as "selected" rather than "pinned" and visually competes with real selection states; and the user menu's header row only said "My account" under the name, with no sense of who has extra privileges.
- **Options for filters** — Leave the fieldsets in the page flow but add padding (still reads as loose content, not a control). Move filtering into the "New request" pattern, a full dialog (too heavy for something used many times per session). Group the fieldsets into a small anchored popover card, matching the existing `fh-menu` panel language.
- **We picked** — A popover card (`filters-panel`) anchored under the Filters button, using the same elevation, corner radius and open/close motion tokens as `fh-menu`'s panel, with its own header (title, active count, "Clear filters", a close icon-button) and closing on outside click via the existing `ClickOutside` directive — the toggle button and the panel share one `fhClickOutside` host so re-clicking the toggle to close it does not fight the outside-click listener.
- **Options for the pin marker** — Keep the full primary-container background. Drop the tint entirely and keep only the corner badge (loses the at-a-glance "this row is different" cue in a dense grid). Replace the full wash with a thin top accent bar plus a smaller, non-overlapping corner badge.
- **We picked** — A 3px primary-colored bar across the top of the card plus a `corner-medium` (not circular) badge inset from the edge, with the chip row given right-padding so it never runs under the badge.
- **What we get** — Pinned cards are now visually distinct without being mistaken for a hover/selected state, and the filters read as a control surface instead of loose form fields. Cost: the filters popover is anchored (`position: absolute`) rather than a native `<dialog>`, so it does not get true focus-trapping for free — a keyboard user can still tab past it into the rest of the page while it stays open. Outside click, its own close button, and an `Escape` handler on the group all close it, but nothing pulls focus back in if the user tabs out instead. That remaining gap is written here rather than hidden.
- **User menu** — the header row now shows the role as a small tonal badge next to "My account" when the signed-in user is an admin, and the trigger became a tonal pill (background at rest, not text-only) so the account control reads as a control, not decoration, matching the Filters and New request buttons beside it.

### D-58 — `fh-menu-item`'s label was empty for every item that navigates, because the same content was projected through two `<ng-content>` outlets

- **Problem** — A screenshot of the running app showed the user menu's "Settings" and "Admin" rows as a bare icon with no text, while "Sign out" showed its text fine. `MenuItem`'s template had two branches — `<a>` for a `routerLink` item, `<button>` for an action item — each with its own `<ng-content>` for the label. Angular only fills the ng-content instance it resolves as the real projection target when a template declares more than one with the same (empty) selector; the other stays permanently empty. Written as an `@if`/`@else`, that meant every navigating item (Settings, Admin) lost its label, and only action items (Sign out) kept theirs — confirmed by a throwaway test rendering both branches directly and reading back `innerHTML`.
- **Options** — Always render `<a>`, even for items with no destination (wrong semantics: an anchor with no navigation misleads keyboard and screen-reader users, and Space no longer activates it). Duplicate the label as two literal `@if`/`@else` copies of the same static string instead of projected content (defeats the point of `<ng-content>`, and still two outlets). Move the shared markup into one `<ng-template>` and stamp it into either host element with `*ngTemplateOutlet` (one physical `<ng-content>`, either host renders it).
- **We picked** — The `<ng-template>` + `ngTemplateOutlet` version.
- **What we get** — One `<ng-content>` in the whole template, so every item's label renders regardless of which branch it takes; `menu.spec.ts` now renders one `routerLink` item and one action item side by side and asserts both keep their accessible name, so a regression here fails a test instead of only showing up in a screenshot. Cost: one more moving part (`ng-template`/`ngTemplateOutlet`) in a component that used to be two flat branches — worth it for a bug this easy to reintroduce by accident.

---

## Comments and navigation

### D-73 — Deleting a comment removes the row, not R-38's grey line

- **Problem** — The SRS (R-38, R-39, R-41) says a deleted or rejected comment stays in the thread as an empty "grey line", uncounted. Seen in the running app, that line reads as a bug — a broken, blank card — and it also tells everyone that a comment used to be there and who wrote it. The owner asked for a real delete: gone from the thread and gone from the database.
- **Options** — Keep R-38's grey line. Hard delete the row. Soft delete: keep the row in the database but hide it from every reader (still leaks nothing on screen, keeps a paper trail, but the "reply that quoted it" problem is the same and the thread still needs special-casing).
- **We picked** — Hard delete. `DELETE /v1/comments/:id` and admin reject both remove the row (`CommentRepository.remove`). The list and the count no longer have a "deleted" case. The front end drops the row from the thread in place, no reload.
- **What we get** — A thread with no broken-looking blank cards, and a delete that is actually a delete. The count logic gets simpler (no carve-out). Cost: this is a deliberate departure from R-38/R-39/R-41 — written here, in `SCOPE.md`, and in `README.md`. A reply that answered a now-deleted comment loses that context. The Postgres `CommentState` enum keeps its unused `deleted` value rather than take a fiddly enum-shrinking migration.

### D-74 — A breadcrumb on every page below the board

- **Problem** — The deeper screens (a request, a settings tab, an admin section) gave no sense of where you were or how to step back up, beyond the browser Back button. Not raised in the brief.
- **Options** — Do nothing (rely on Back and the header logo). Add a per-page "← Back to the board" link (one level only, no hierarchy). A real breadcrumb trail from the board down.
- **We picked** — A shared `fh-breadcrumbs` component, fed a small list per page. The last item is the current page as plain text with `aria-current="page"`; every earlier step is a link. The admin shell derives its section label from the active child route.
- **What we get** — Orientation on every deep page, and a consistent way back, for a small component. Cost: one more thing each new page must remember to add; there is no route-config-driven automation, so a page with no breadcrumb simply has none.

### D-75 — Every control on the request page sits on the thing it acts on: no admin bar, no vote footer

- **Problem** — The detail page had grown into stacked boxes with nothing reading as one object. Four passes followed: a tinted "Admin" strip; a right-aligned footer row; folding the controls into the header as a native `<select>` plus a corner pin; and finally this one. The `<select>` still read as a form control dropped into the page, and the vote pill under a divider still read as its own separated section.
- **Options** — Keep the native `<select>` and the divided vote footer. Move the vote into an overflow menu. Make the status a proper menu picker and lift the vote onto the same row as the status.
- **We picked** — Status is a `fh-status-menu` (new component): a chip-shaped button showing the current status + colour dot, opening the shared `fh-menu` with every status and a tick on the current one. Non-admins still see a plain `fh-taxonomy-chip`. The **vote button is a full-width bar** across the bottom of the card, below the description — a filled primary "Upvote · N" that can't be missed, dropping to a quiet outline ("Voted · N") once cast. Six earlier placements were tried and rejected by the owner (left rail, small pill on the meta row, a divided footer, left of the title, in the flow after the description, stacked under the corner icons); the full-width bar was picked from a mock-up.
- **What we get** — Changing status feels like changing status (pick from a list, see the tick move), not editing a form field. The vote reads unmistakably as the call to action. The card is one surface with one border. Cost: `fh-request-header` now takes admin inputs/outputs (no longer purely presentational); a new `requestDetail.changeStatusLabel` string and a `fh-status-menu` component to maintain; and the vote button inverts the usual emphasis (filled = not yet done), which is deliberate but unusual.

### D-76 — Editing a request opens as a popup on the request page, and the `/requests/:id/edit` route is removed

- **Problem** — The Edit control on the request page was a link to `/requests/:id/edit`. That route loaded `RequestFormPage`, which is just a thin wrapper that forces the shared `fh-request-form-dialog` open and navigates back on close — so clicking Edit swapped the whole request page for a near-empty page whose only content was the dialog. The owner asked for it to be a popup only, like "New request" already is (D-52).
- **Options** — Keep the route and just style the wrapper page better. Keep the route as a deep-link fallback (forced-open dialog) the way `/requests/new` is kept, but also mount the dialog on the request page. Remove the edit route entirely and mount the dialog on the request page, reachable only from there.
- **We picked** — Remove the route. `fh-request-detail` now hosts `fh-request-form-dialog` itself: the header's pencil is a `fh-icon-button` emitting `editRequested`, which sets an `editing` signal; on save the detail reloads in place and a snackbar confirms. `/requests/new` still keeps its forced-open dialog page, because a "new request" link is worth sharing; an edit link for one specific request is not.
- **What we get** — Editing never leaves the request you are editing; scroll position and the comments below are undisturbed. One less route and the `RequestFormPage` wrapper is now only used for `/new`. Cost: an edit form is no longer reachable by URL, so a bookmarked `/requests/:id/edit` now 404s (it was never a documented or shared link). `fh-request-header` gains one more output. Two Cypress specs that clicked `a` "edit" now target `button[aria-label="Edit"]`.
- **Follow-up — the form dialog dropped its own Delete button.** The dialog used to carry a red "Delete" beside Save/Cancel (with its own confirm step, D-53). With edit now opening right on the request page, that button sat inches below the header's trash icon (D-65), which does the same thing through the same `ConfirmService` dialog — two delete controls for one request, one of them inside an "edit" form where deleting is not editing. Removed it: the dialog is create-and-edit only, and the header trash icon is the single place a request is deleted. `fh-request-form-dialog` loses its `deleted` output, its `askToDelete`/`ConfirmService` wiring, and the `RequestFormPage` wrapper loses the matching `onDeleted` navigation. `RequestFormStore.remove` stays (still covered by its own store spec) in case a delete affordance is ever wanted back here.

### D-77 — `fh-skeleton-rows` grew a `layout="stack"` so the request page's loading state is not a lone third-width card

- **Problem** — `fh-skeleton-rows` was built for the board: its `.skeleton-grid` fans cards into 1, 2, or 3 columns as the viewport widens (matching D-72's real grid). The request detail page and the comments list reused it with `count="1"` and `count="3"` — so on a desktop width the request skeleton rendered as a single card taking only the left third of the page, with empty space beside it, and the comments skeleton as a 3-across row. It read as broken layout, not as loading.
- **Options** — Build a bespoke skeleton component per page. Drop the shared skeleton on these pages and inline a few `.line` divs. Add one input to the shared component that switches the grid to a single full-width column.
- **We picked** — The input. `layout` defaults to `'grid'` (every existing board/admin use is unchanged); `layout="stack"` forces `grid-template-columns: 1fr` at all widths. The request page and the comments list pass `stack`.
- **What we get** — The request page's loading state now fills the column the article will fill, and comment placeholders stack the way comments do, for a one-line CSS rule and one optional input. Cost: `fh-skeleton-rows` now has two shapes to keep in mind, and `.skeleton-card`'s internal line pattern (a short line, then body lines) still reflects a board card rather than a request header + description — close enough that it was not worth a second template.

### D-78 — The personal settings page is titled "Profile", and the "lives on this device" explainer is dropped

- **Problem** — The page a person opens from the user menu was titled "Settings", but its main job is the profile (name, picture), with language, email and browser choices under it. Next to the admin "Application settings" screen, two things called "settings" invited confusion. The "This browser" card also carried a sentence — "Theme, default sort and default filters live on this device, not on your account." — that restated its own heading in more words.
- **Options** — Keep "Settings". Rename to "Account". Rename to "Profile". For the explainer: keep it (D-06 says surface where config lives), shorten it, or drop it and let the "This browser" heading carry the meaning.
- **We picked** — Title the page, the user-menu link and the browser tab "Profile". Rename the first card from "Profile" to "Name and picture" so it does not echo the page title. Drop the explainer sentence; the "This browser" heading and the card grouping already say it.
- **What we get** — One page called "Profile", one admin page called "Application settings", no overlap. Less text on the screen. Cost: `settings.deviceDetail` is gone and one unit test that asserted the sentence was removed; the folder and store are still named `settings` (the store talks to `/v1/settings/me`), so that name in the code and the name on the screen no longer match.

### D-79 — The Profile page moves to `/profile`, and is rebuilt as an identity header + section rail + card stack

- **Problem** — D-78 renamed the page but left it at `/settings`, so the URL still disagreed with the name. The owner also asked for the whole page and every element on it to be reworked. The old page was a bare `<h1>` over four full-width cards in one column — no sense of who you are, no way to jump between sections, the save button and "Saved" note floating loose at the bottom of each form, toggles as a cramped icon-left list, and the danger zone built from ad-hoc markup instead of the shared card.
- **Options** — Leave the route, restyle in place. Rename the route and keep `/settings` as a redirect. Rename the route with nothing left behind. For layout: keep one column and just tidy spacing; or add a settings-style left rail. For the design source: run `ui-ux-pro-max --design-system` and adopt what it returns (a "Vibrant & Block-based" landing style on Plus Jakarta Sans); or keep the project's Material 3 system (D-52, D-67) and apply only the skill's UX rules (hierarchy, grouping, helper text, feedback, touch targets, heading order, reduced-motion).
- **We picked** — Route is `/profile`, and the old `/settings` path is simply removed — no redirect. A stale `/settings` link now hits the normal not-found page. The page keeps the M3 design system and takes the skill's UX guidance. New shape: an identity header (avatar + "Profile" + admin badge + one-line subtitle) over a two-column grid on `lg` — a sticky in-page nav listing the four sections — each with the matching section icon and a "you are here" highlight driven by an `IntersectionObserver` scroll-spy — and the card column (capped at 42rem). `fh-section-card` gained an `icon` slot and a `tone="danger"` variant; the danger zone now uses it like every other section. Each form ends in a footer action bar — a full-width top divider (a horizontal `-mx-6` bleed) over the submit button and an inline "Saved"/error line with an icon, with the card's own bottom padding left intact so the button is never flush to the edge. The nav links are `routerLink="/profile"` + `[fragment]`, so a click keeps the `/profile` path and reads `…/profile#account`. Email toggles became rows with a title, a one-line explanation, and the switch on the right. The device-scoped card, once "This browser", is now **"Appearance and defaults"** — it reads as a thing you set, not a place, and matches the "X and Y" shape of the other card titles. `scroll-behavior: smooth` and a `scroll-padding-top` of the header height are set site-wide (the first behind `prefers-reduced-motion`), so anchor jumps clear the sticky app header; the rail itself sticks at the same offset, inside a plain wrapper cell so the grid row can stretch it tall enough to travel.
- **What we get** — The page reads as one thing about one person, sections are reachable in one click, and every card ends the same way. The section-card changes are shared, so the admin cards pick up the tighter heading and the icon slot for free. Costs: four `<section>` landmarks on the page now instead of one (deliberate — each is a nav target); the scroll-spy needs `IntersectionObserver`, so it is guarded with a `typeof` check and simply does nothing in the jsdom unit-test env; the footer action bar leans on a negative horizontal margin to bleed the divider to the card edge; and the Cypress `07-personal-settings` spec still targets the pre-D-77 DOM (`select#theme`) and was not rewritten (D-51).

### D-80 — The three admin screens are rebuilt on the profile page's card system, and the taxonomy table is dropped

- **Context** — Shown the admin area in a browser and told it "not show correctly and design is too bad", with the profile page (D-79) as the bar to hit. The categories/statuses screen was visibly broken: the columns collapsed, every chip landed on one line, and the "In use by" / "Actions" headers floated off to the right with nothing under them. The cause was structural — `taxonomy-admin.html` put `<fh-taxonomy-row>` elements straight inside `<tbody>`, and each of those components renders a `<tr>`. A custom element between `<tbody>` and `<tr>` is not valid table structure, so the browser hoisted the cells out and the table lost its grid. The settings and invitations screens were not broken, just plain: bare section headings, switches as a cramped `switch + label` line, six rate-limit inputs in an unlabelled 2-by-3 grid, and an HTML `<table>` for two columns of invitation data.
- **Options** — (1) Keep the `<table>` and fix only the wrapper — give `taxonomy-row` `:host { display: contents }` so the `<tr>` reparents to the `<tbody>`. Narrowest change, but `display: contents` on table rows is still patchy across engines, and the other screens stay plain. (2) Move the `<tr>` markup up into `taxonomy-admin.html` and keep `taxonomy-row` as a pure logic holder with no template. Keeps the table, loses the reusable row. (3) Drop the table idea for all three screens and rebuild them the way the profile page now works — the same two-column shell (sticky section rail + card column), `fh-section-card` with an icon and a one-line detail, content as divided rows of `label + hint + control`, add-forms as a bleed footer. More markup touched, but the two areas end up on one system.
- **Decision** — Took option 3, over three rounds of the owner sending screenshots back. `admin-shell` was restructured to mirror `settings.html` exactly: an identity-style header (a filled icon circle + "Admin" + one-line subtitle) over `lg:grid lg:grid-cols-[13rem_minmax(0,46rem)]`, with the section links as a sticky vertical rail on the left (each with its section icon and the same secondary-container active pill the profile rail uses) that turns into a horizontal scroll strip below `lg`, and `<router-outlet />` in the right column. The screens stay separate routes — only the chrome around them changed. `taxonomy-row` is a dense single-line row: colour chip, an optional "Default" pill, and a muted "Used by N" / "Not used yet" hint, all inline on the left; on the right the actions are **text buttons** — `Make default`, `Retire` / `Make active`, and `Delete` (shown only when nothing uses the row, in the error colour). An earlier round of this decision put those actions behind a `⋯` menu; the owner said explicitly to keep the buttons visible, so they are back as words — the first version's problem was that they were unlabelled icons (`×`, check, pin, trash), not that they were buttons. A new `danger-text` variant was added to `fh-button` for the destructive one. Rows sit in a `<ul>` with a top border on each `:host`. `taxonomy-form` renders as a card footer (`-mx-6 -mb-6 border-t px-6 pt-5`) with a small uppercase caption, a "Name" field, a compact colour well, and a `+ Add` button — the "Add category" label that used to appear three times now appears once. `comments-card` switches became profile-style rows (title, one-line explanation, switch on the right). `registration-card` shows the meaning of the current policy as the field hint. `rate-limits-card` renders each limit as its own `<fieldset>` with a legend and a two-field grid ("How many" / "In how many minutes"), driven by a small `limits` array instead of six copy-pasted blocks. `invitations` is two cards — the invite form in one, the sent list in the other — and `invitation-table` is a `<ul>` of rows (address, "Waiting" or an "Accepted <date>" line in green, and a `Withdraw` text button). New i18n keys were added to both `en.ts` and `ar.ts`. Verified by driving the running app with a throwaway Cypress spec and reading the screenshots, not by eye alone.
- **Consequences** — Good: the broken screen renders correctly; the admin area now uses the profile shell verbatim, so the two read as one product; every row action is a labelled button, not a glyph; the rate-limit fields are finally labelled; the rail collapses cleanly on a phone. All 255 web unit tests still pass and the build is clean — no spec targeted these components by their old DOM. Bad: a lot of template churn for what began as a one-line layout bug; the `<table>`s are gone, so the invitation and taxonomy lists are no longer semantic tables for a screen reader (they are labelled lists now — defensible for this little data, but a real change); a status row with every action available shows three text buttons in a run, which is busy (they wrap on a narrow card); `taxonomy-form`'s footer leans on the same negative-margin bleed trick D-79 already flagged as fragile; `fh-button` grew a variant for one caller; and the Cypress admin specs were not re-checked against the new DOM (D-51). Also noticed in passing: the shared `signIn` E2E command still asserts on an `<h1>` containing "Feedback" that the board no longer renders — left alone as out of scope, but it will fail the suite until fixed.

### D-81 — Categories and Statuses become their own screens; adding one is a popup opened from a top-right button

- **Context** — After D-80 the owner asked for three more things on the admin area: (1) Categories and Statuses should be *separate* screens, not two cards stacked on one "Categories and statuses" page; (2) adding a category or status should be a button that opens a popup, not a form sitting permanently at the bottom of the card; (3) an "Add" button in the card's top-right corner; (4) the loading skeletons, still the board's three-column card grid (`fh-skeleton-rows`), should match the new layout.
- **Options** — For the split: two separate components, or one component parametrised by the route. For the add form: a route (`/admin/categories/new`), an inline expand/collapse, or a modal dialog. For the skeleton: tune `fh-skeleton-rows` (add yet another `layout`), or a purpose-built card-shaped skeleton.
- **Decision** — One `TaxonomyAdmin` component, told which list it is by a `kind` route input (`data: { kind: 'categories' | 'statuses' }`, bound through the router's `withComponentInputBinding`). Routes are now `/admin/categories` and `/admin/statuses`; `/admin` and the old `/admin/taxonomy` both redirect to `/admin/categories`. The admin rail gained a second entry, so it now lists Categories · Statuses · Application settings · Invitations, each still a real route with its own title and breadcrumb. `fh-section-card` grew a projected `[card-action]` slot rendered at the top-right of the card head (a flex row, `justify-content: space-between`; the slot `display: none`s itself when nothing is projected, so every other caller — the whole profile page — is unaffected). The screen puts a small `+ Add category` / `+ Add status` button there. Clicking it opens `fh-taxonomy-dialog`, a new `sm` modal built on the shared `fh-dialog` with the name field (now marked required) and the colour well, a Cancel/Add footer, and an `effect` that resets the form on every open. The old `fh-taxonomy-form` footer component was deleted. Loading now uses a new `fh-skeleton-card` (shared, in `state/`): it renders one or more boxes shaped exactly like an `fh-section-card` — icon dot, title line, detail line, divider, then N rows each with a chip-shaped blob and an action blob. All four admin screens (categories, statuses, settings, invitations) and the waiting-comments screen switched to it with counts that match what they actually show; `fh-skeleton-rows` stays for the board and the request page.
- **Consequences** — Good: the two lists are genuinely separate and each is a shareable URL; adding one is out of the way until asked for, and the dialog gives it a heading, a required-field marker, and a real Cancel; the skeleton finally previews the right shape instead of flashing a board grid; `[card-action]` is a general slot other cards can use later. Bad: `/admin/taxonomy` is now a redirect, and the E2E specs that still `visit('/admin/taxonomy')` and expect both `<h2>`s on one page (08, 14) will need updating along with the D-80 changes they were already stale against (D-51); one component now branches on `kind` with a `text()` lookup table and a few `$any(item)` casts across the category/status union, which is slightly awkward; and there are three more shared components / slots to keep in mind. Verified by driving the running app and reading screenshots of both lists, the popup, and the delayed-response skeleton state; all 255 web unit tests still pass and the build is clean.
- **Follow-up — the rows became a real aligned table.** First cut of the split screens laid each row out with flex, so "Retire" landed at a different x on every row depending on whether "Delete" or "Make default" was also there — it read as ragged, not as a table. Reworked it into a CSS grid: `.tax-table` is `grid-template-columns: minmax(9rem,max-content) minmax(7rem,max-content) 1fr` with a header row of column labels (Name · In use by · Actions), and `fh-taxonomy-row` is `:host { display: contents }` so its three cells drop straight into that grid and every column lines up across every row. The actions cell is itself a fixed sub-grid (`5.5rem 5rem`, or `6.5rem 5.5rem 5rem` on the statuses screen which also has "Make default"), so each action keeps its own column and an absent one leaves a gap rather than shifting its neighbours. Below 40rem the grid collapses to one column and the cells stack (name, then the usage line, then the actions) — the source order already matches. The default status now also hides its "Retire" button (R-48), not just relies on the server 409. ARIA table roles were tried and dropped: `display: contents` plus `role` is still buggy for screen readers, so the block is a labelled `role="group"` with a visually-hidden header row instead.

### D-82 — Admin actions stop flashing the skeleton, the default-status marker is restyled, and Application settings gets a Save button

- **Context** — Three complaints about the admin screens after D-81. (1) Every action — retire a category, make a status default — briefly replaced the whole list with the loading skeleton, because the re-read after the action ran through the same code path as the first load and reset the state to `loading`. A skeleton that flashes for 150ms on a button press reads as a glitch. (2) The "Default" marker on a status was a grey pill sitting right next to the coloured chip — it looked like a second, broken chip. (3) Application settings saved on every keystroke and every toggle: one click on a switch fired a PATCH. The owner wanted an explicit Save, so a half-considered change isn't instantly live.
- **Options** — For the flash: a spinner overlay instead of the skeleton; keep the skeleton but only on first load; or a separate "refreshing" flag. For the default marker: a star/pin icon; a coloured pill; a plain text label; move it to its own column. For settings: a Save button per card (like the profile page); one Save button for the whole screen; a sticky save bar.
- **Decision** — (1) `AdminStore.read()` took a `keepShowing` flag: when the screen is already `ready`, the re-read after an action leaves the current rows and state exactly where they are and just swaps the data in on success (and on a re-read failure keeps showing the old, still-correct data rather than blanking to an error — the action itself worked). Every `act()` reload now passes it; the first `load*()` call in each screen's constructor does not, so the real first-load skeleton is unchanged. (2) The default marker is now a small primary-coloured uppercase label — a check-circle icon plus the word "DEFAULT" — with no background, so it reads as an annotation on the row, not a competing chip. The default status also hides its "Retire" button now (R-48), where before it relied only on the server 409. (3) Application settings holds a local `draft` (a `linkedSignal` cloned from `admin.settings()`, so it re-seeds on load and after a successful save). The three cards are unchanged — they were already controlled components taking `[settings]` and emitting granular changes — but the page now folds those changes into the draft instead of saving them. A sticky bar at the bottom carries **Save changes** (disabled unless the draft differs from the server and every rate limit is ≥ 1), **Discard** (shown only when dirty), and the "unsaved changes" / "Saved" / error line. `rate-limits-card` lost its own `tooSmall` signal — it now emits every keystroke (even a 0) so the draft and the page-level guard see it, and marks the field `aria-invalid`; the full "a limit must be at least 1" explanation shows once, under the card.
- **Consequences** — Good: actions feel instant — the row just updates; settings changes are reversible until Save, and the bar makes the unsaved state obvious; the default marker no longer competes with the chip. `keepShowing` is a small, contained change that also quietened the settings, invitations and waiting-comments screens. All 255 web unit tests pass and the build is clean; the store spec's "leaves the rows alone when an action fails" and "keeps the old values when a save fails" both still hold. Bad: `AppSettings` is now a stateful component with a draft, a dirty check (a `JSON.stringify` compare) and a validity check, where it used to be four one-line handlers; a re-read failure after a successful action is now silent (the toast/So the person sees success even though the list may be a few seconds stale until the next navigation) — judged the right trade for not blanking the screen; and the E2E specs for settings (09) and taxonomy (08, 14) are now further out of step with the DOM and the save flow — `.blur()` no longer auto-saves, the invitations table is a list, `/admin/taxonomy` is a redirect — they need a pass (D-51). Verified by driving the running app: watched for a skeleton during an add (none), and screenshotted the new marker, the dirty bar, the invalid state, and the saved state.

### D-83 — Taxonomy columns are fixed-width, and the Save buttons for Application settings and "Language and email" are removed (back to auto-save, no "Saved" line)

- **Context** — Three follow-ups. (1) The Categories and Statuses tables used `max-content` for the Name and "In use by" columns, so the two screens sized those columns to their own content and didn't line up with each other. (2) The Application-settings Save button from D-82 was not wanted — the owner asked to go back to save-on-change, but *without* the "Saved" banner. (3) Same for the profile page's "Language and email" card, which still had a "Save language and email" button.
- **Decision** — (1) `.tax-table` is now `grid-template-columns: 14rem 9rem 1fr` — fixed, so Name and "In use by" are the same width on both screens; the Actions column takes the rest (its content still differs because Statuses has "Make default"). The stack-to-one-column breakpoint moved from 40rem to 48rem, since three fixed columns get tight before the layout is actually phone-width. (2) `AppSettings` went back to the pre-D-82 shape: four one-line handlers that each call `admin.saveSettings({ field })` on change, no draft, no sticky bar. The `@if (admin.wasSaved())` "Saved." line was removed from the template (the store still tracks the flag; nothing renders it). `rate-limits-card` got its `tooSmall` guard back — a value below 1 is not sent and the card shows the reason. The D-82 skeleton-flash fix (`keepShowing` on the re-read) stays — that was a separate concern and still wanted. (3) `account-form` lost its `<form>`, its Save button and its `store.settingsSaved()` "Saved" line. Language and the two email toggles now persist the moment they change (`onLanguage`, `toggleComment`, `toggleStatusChange` each call a shared `persist()`), and only the error line remains. `profile-form` keeps its Save button — it has a free-text name and picture URL, where saving on every keystroke would be wrong.
- **Consequences** — Good: the two taxonomy screens visually agree; settings and the language card are back to the simpler direct-save model the owner prefers, with no interrupting confirmation. All 255 web unit tests pass and the build is clean — `settings.spec.ts` only ever exercised `profile-form`, so dropping the account-form Save button broke nothing. Bad: this reverses D-82's settings half two rounds after adding it (the skeleton-flash and default-marker parts of D-82 stay); "each part says 'Saved'" from SRS 15.6 is now not honoured for these cards — a deliberate call, the owner has overridden that guidance twice now (see also D-78); a failed auto-save still shows its inline message but there's no positive signal that a change stuck, which is the trade-off of removing the banner; and the E2E specs (08, 09, 14) drift further — 09 in particular asserts on a per-field save confirmation that no longer exists (D-51).

### D-84 — The request form popup shows its title, and category is a radio-pill group, not a native `<select>`

- **Problem** — Shown the "New request" / "Edit request" popup and told the design was not good, the category control especially. The popup had no visible heading (the `<h2>` was `sr-only`, so the dialog opened with three fields and no title). Category was a bare native `<select>` — the one element the board toolbar rework (D-77) had deliberately turned into chips everywhere else — with no chevron of its own, no invalid styling, and a "Choose a category" placeholder option that could itself be submitted. The footer buttons were left-aligned and ran against the project's other dialogs (taxonomy, confirm), which put Cancel then the primary action on the right over a divider.
- **Options** — For the heading: keep it `sr-only` and pass `labelledBy`, or use `fh-dialog`'s own visible `[heading]` (which every other dialog uses) and wire `aria-labelledby` to it. For category: restyle the `<select>` (custom chevron, drop the placeholder, add an invalid border); a custom listbox/combobox component; or a radio group rendered as pills — one real `<input type="radio">` per active category inside a `<fieldset>`, visually a chip with the category's colour dot. For the footer: leave it, or match the taxonomy/confirm dialog pattern.
- **We picked** — Pass `[heading]` to `fh-dialog` and give the shared dialog a small change: it now auto-generates an id on its visible title and points `aria-labelledby` at it whenever no explicit `labelledBy` is set — so the request form (and every other heading-only dialog, which had no accessible name before) is now labelled. Category is a radio-pill group: a `<fieldset>` with a `<legend>`, a `role="radiogroup"`, and one `.category-option` label per `bootstrap.activeCategories()` — hidden radio input, colour dot, name, and a check icon when selected; selected state uses the same secondary-container fill as the board filter chips. The value is written through the signal-forms field (`f.categoryId().value.set(...)` + `markAsTouched()`). Native `<select>`s elsewhere (registration policy, default sort) also got a custom chevron and an invalid state, since `.fh-select` is shared. Footer now matches the other dialogs — Cancel (text) then Save, right-aligned, over a top divider. The popup size is unchanged (`size="lg"`).
- **What we get** — The popup has a title again; every category is visible at once with its colour, arrow keys move between them, there is no hidden menu and no submittable placeholder; the form matches the rest of the app's dialogs. Every heading-only `fh-dialog` gained an accessible name for free. Costs: the `select#categoryId` is gone, so the unit spec and four Cypress specs (01, 04, 08, 14) that drove it with `.select()` were updated to pick a radio instead — the wider E2E drift from D-51 is still there; a project with many categories would show a tall wrap of pills where a dropdown stays compact (fine for the handful this product has); and `fh-dialog` now carries a module-level counter for its title ids.

### D-85 — A saved change is written back into the start-up snapshot, so the whole UI updates without a browser refresh

- **Context** — Reported: after changing the display name on the Profile page, the header and the user menu kept showing the old name until the browser was refreshed; the owner asked for the same fix everywhere — any save, add, delete or state change should update every related part of the screen with no reload. The cause was structural. `GET /v1/bootstrap` is fetched once at start-up (R-52) and its answer — the viewer, my settings, the two comment switches, the taxonomy — is held in `BootstrapStore` as the app-wide source of truth that the header, the user menu, the board filters, the category picker and the status labels all read from. But the stores that *change* those things (`SettingsStore` for profile and my settings, `AdminStore` for taxonomy and app settings) made their own calls and never told `BootstrapStore` what changed, so its copy went stale until the next full page load. Feature-local views were already fine — the board re-loads after a create or delete, the comment thread inserts the saved row, request-detail takes the server's answer as the new row — this was specifically the shared snapshot.
- **Options** — (1) After any mutation, re-call `GET /v1/bootstrap` and replace the whole snapshot. Simple and always correct, but a full round trip for a one-field change, and it re-fetches the taxonomy and settings on every profile edit. (2) Make `BootstrapStore` a live store other stores push into: add narrow merge methods (`applyUser`, `applyMySettings`, `applyFeatures`, `applyTaxonomy`) that each mutating store calls with the server's answer to its own write. (3) Move every piece of shared state out of `BootstrapStore` into per-domain root stores and have screens read from those. Cleanest long-term, but touches every consumer.
- **We picked** — Option 2. `BootstrapStore` gained four `apply*` methods that merge a partial into the held snapshot (each a no-op before the first load). `SettingsStore.saveProfile` calls `applyUser` with the saved name and avatar; `saveSettings` calls `applyMySettings`. `AdminStore` calls `applyTaxonomy` inside `loadTaxonomy` and `applyFeatures` inside `loadSettings`, so every admin action that re-reads (all of them) also refreshes the snapshot. The account-form language switch now calls `i18n.setLanguage` on success so the app text flips immediately, not just the stored preference.
- **What we get** — Renaming yourself updates the header and menu at once; an admin retiring a category drops it from the board filter and the request-form picker straight away; turning comments off hides the comment section and the "waiting comments" admin tab immediately. No extra network calls — each write already had its answer, or (for admin) already re-reads. Three new unit tests in `settings.store.spec.ts` and two in `admin.store.spec.ts` lock the write-back; all web unit tests pass and the build is clean. Costs: `BootstrapStore` is no longer a write-once cache — it has a small mutable surface, and a mistake in a merge shows up app-wide rather than on one screen; `SettingsStore` and `AdminStore` now depend on `BootstrapStore`; the taxonomy write-back maps the admin row shape down to the smaller bootstrap shape by hand, so a new bootstrap taxonomy field needs adding in that mapper too; and `AdminStore`'s reads now write to a root store as a side effect, which the tests that use a real `BootstrapStore` have to account for. Option 3 (per-domain stores) is still the right destination if this surface grows.

### D-86 — The board toolbar writes back the default sort and filters, so the board reopens where you left it

- **Context** — Asked for the board's sort dropdown and its filter chips (status and category) to "save state in local storage" and to "take the same value always" — i.e. the board should reopen with the last sort and filters this browser used, not reset to Newest with no filters every visit. The pieces were already there: `DevicePreferencesStore` persists a default sort and default status/category filters to `localStorage` (D-06), and `resolveBoardQuery` seeds the board from them when the URL is silent (R-24). Only the write was missing — those defaults could be set from the Profile page's "Appearance and defaults" card but never from the board itself.
- **Options** — (1) Add a separate second `localStorage` key just for "last used" board state, kept apart from the Profile defaults. Two sources for the same thing, and the Profile card would then be lying about what the board actually opens with. (2) Have the board toolbar write straight into the existing `DevicePreferencesStore` defaults — one key, one meaning, the Profile card and the toolbar edit the same value. (3) Leave it: the URL already carries the state, so a bookmark remembers it.
- **Decision** — Option 2. `Board.onSort` now calls `preferences.setDefaultSort(sort)`, `Board.toggle` calls `setDefaultStatusIds` / `setDefaultCategoryIds` with the new list, and `clearFilters` clears both, each before it navigates. The URL still carries the query and still wins while it is on screen (R-24, unchanged) — this only changes what a clean visit to `/board` falls back to. Search text is deliberately not remembered: it is a one-off lookup, not a standing view, and the Profile card has never offered it. `board.spec.ts`'s `DevicePreferencesStore` stub gained the three setter mocks; all 20 board specs pass and the build is clean.
- **Consequences** — Good: the board reopens exactly as you left it, from any entry point, with no bookmark needed; there is still one place the default lives, so the Profile card and the toolbar agree; nothing about the URL-wins precedence changed. Bad: changing a filter on the board now silently rewrites a "default" the person may think of as belonging to the Profile page — there is no undo beyond changing it back; every sort click and chip toggle now does a `localStorage` write (cheap, wrapped, but it is a side effect on a hot path); and a shared machine now carries one person's board filters into the next person's session until they change them (the same trade-off D-06 already accepts for theme and sort).
- **Follow-up — the card gained a "Default statuses" chip group.** `DevicePreferencesStore` has always held `defaultStatusIds` (D-06) and the board seeds from it, but the "Appearance and defaults" card only exposed categories, so a default status filter could be set from the board toolbar (D-86) yet not seen or cleared on the Profile page. Added a second `<fieldset>` in `device-preferences-form` — same `fh-filter-chip` group, driven by `bootstrap.activeStatuses()`, toggling `setDefaultStatusIds`. New i18n keys `settings.defaultStatusesLabel` / `defaultStatusesHint` in `en.ts` and `ar.ts`. The `settings.spec.ts` bootstrap and preferences stubs gained `activeStatuses` / `defaultStatusIds` / `setDefaultStatusIds`; all settings and i18n specs pass.

### D-87 — The router outlet renders while signed out, so `/sign-in-problem` is not a white page

- **Context** — `/sign-in-problem` is a public route, outside the shell, with no guard. But `app.html` switched on `bootstrap.status()` and only put `<router-outlet />` inside `@case ('ready')`. On that page the visitor is not signed in, so `/v1/bootstrap` returns 401 and the status is `signedOut` (D — bootstrap store) — which matched no `@case`, so nothing rendered at all. The page the identity server redirects a failed sign-in to was a blank screen. Also found: `SignInProblem.heading()` only matched `problem === 'cannot_join'` exactly, so `?problem=cannot_join_yet` showed the heading "Signing in failed" over a body about the system being busy.
- **Options** — (1) Special-case the public routes: keep rendering nothing for `signedOut`, but detect when the current URL is one of the public paths and render the outlet then. Two ideas of "what is public", one in the router config and one here. (2) Render `<router-outlet />` for `signedOut` as well as `ready`, and let the guards do their job — `authGuard` already sends a signed-out person off every protected route to the identity provider (`location.assign`), so the outlet on those routes never draws anything anyway. (3) Move `/sign-in-problem` inside the shell — no, it must work when bootstrap has failed.
- **Decision** — Option 2. `app.html` now has `@case ('failed')` and `@case ('loading')` and a `@default` holding the outlet, so `ready` and `signedOut` both render it. `SignInProblem` gained an explicit `cannot_join_yet` branch with a new heading key `signInProblem.headingCannotJoinYet` ("You cannot join yet") in `en.ts` and `ar.ts`. The two other full-page message screens, `not-found` and `not-allowed`, were top-left aligned with a plain underlined link while `sign-in-problem` was centred with a filled button — all three now use the same centred `min-h-dvh` layout and the pill button. `app.spec.ts`'s signed-out test was reworded: it still asserts no "could not start" heading and no spinner, which is the actual rule (`signedOut` is not an error, SRS 15.8). All 7 `app.spec.ts` tests and the 10 i18n tests (including the Arabic/English key-parity check) pass.
- **Consequences** — Good: the sign-in problem page works; the three message pages look like one product; the guard stays the single place that decides what a signed-out person may see. Bad: for a brief moment on a protected route, a signed-out person has the outlet mounted before `location.assign` takes the browser away — in practice nothing renders because the guard returns `false` synchronously, but the "draw absolutely nothing while redirecting" guarantee the old code gave is now "draw nothing worth seeing". `tsc --noEmit` on the web app is clean.
